# UI 打磨（按钮+表单+组件一致化）实现计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 将全站内联样式迁移到"设计令牌 + 全局样式注入"体系，统一按钮/表单/弹窗/表格/toast，并将编辑弹窗从裸 JSON 输入改为结构化表单。

**Architecture:** 新增 `src/ui/theme.js`（`tokens` 设计变量 + `injectGlobalStyles()` 注入 `<style>`）与 `src/ui/fields.js`（`field()`/`rowsEditor()` 表单构建函数）。`main.js` 入口调用 `injectGlobalStyles()`，样式随 esbuild bundle 内联进 `dist/index.html`。各视图从拼 `style.cssText` 改为挂 CSS 类，因此能使用 `:hover`/`:focus`/`:active`/`:disabled` 伪类。

**Tech Stack:** 原生 ES Modules，无框架；Vite（dev）、esbuild（build）、node:test（单测）。

## Global Constraints

- 无框架、纯原生 ES Modules；不引入任何新依赖。
- 算法层 `core/` 保持纯函数无 DOM；本次**不触碰** `core/` 任何文件。
- `build.js` 不改动；样式必须随 JS 内联进单文件，`file://` 双击可用。
- 提交信息遵循项目风格：`[模块] 描述`；只 add 本任务涉及文件，**绝不** `git add -A`（工作区有 Tao 未提交的 db.js 等改动）。
- 所有 UI 产物仅手测（项目约定：views/ui 依赖浏览器）；node:test 只覆盖 model.js 的 validate 结构改动。
- `npm test` = `node --test`；`npm run dev` = vite；`npm run build` = `node build.js`。

---

### Task 1: 主题系统 theme.js + 入口注入

**Files:**
- Create: `src/ui/theme.js`
- Modify: `src/main.js`（首行 import 并调用注入）

**Interfaces:**
- Produces: `injectGlobalStyles()`（无参，幂等，重复调用不重复注入）；`tokens` 对象（后续任务如需引用设计变量）
- Consumes: 无

- [ ] **Step 1: 创建 theme.js（完整内容）**

```js
export const tokens = {
  color: {
    primary: '#2563eb', primaryHover: '#1d4ed8', primaryActive: '#1e40af',
    success: '#16a34a', successHover: '#15803d', successActive: '#166534',
    danger: '#dc2626', dangerHover: '#b91c1c', dangerActive: '#991b1b',
    warning: '#d97706',
    text: '#222', textSecondary: '#6b7280', textMuted: '#9ca3af',
    border: '#d1d5db', borderLight: '#e5e7eb',
    bg: '#f5f6f8', surface: '#fff', surfaceMuted: '#fafafa',
    focusRing: 'rgba(37,99,235,.18)',
  },
  radius: { sm: '4px', md: '6px', lg: '8px' },
  space: { xs: '4px', sm: '8px', md: '12px', lg: '16px', xl: '24px' },
  shadow: { sm: '0 1px 3px rgba(0,0,0,.08)', md: '0 4px 12px rgba(0,0,0,.12)' },
};

const css = `
/* ===== 按钮 ===== */
.btn { display:inline-flex; align-items:center; justify-content:center; gap:6px;
  border:1px solid transparent; border-radius:6px; padding:8px 14px; font-size:14px;
  cursor:pointer; line-height:1.2; user-select:none;
  transition:background-color .15s,border-color .15s,box-shadow .15s,color .15s; }
.btn:disabled { opacity:.55; cursor:not-allowed; box-shadow:none; }
.btn:focus-visible { outline:none; box-shadow:0 0 0 3px rgba(37,99,235,.18); }
.btn-primary { background:#2563eb; color:#fff; }
.btn-primary:hover:not(:disabled) { background:#1d4ed8; }
.btn-primary:active:not(:disabled) { background:#1e40af; }
.btn-default { background:#fff; border-color:#d1d5db; color:#222; }
.btn-default:hover:not(:disabled) { background:#f3f4f6; }
.btn-default:active:not(:disabled) { background:#e5e7eb; }
.btn-danger { background:#dc2626; color:#fff; }
.btn-danger:hover:not(:disabled) { background:#b91c1c; }
.btn-danger:active:not(:disabled) { background:#991b1b; }
.btn-success { background:#16a34a; color:#fff; }
.btn-success:hover:not(:disabled) { background:#15803d; }
.btn-success:active:not(:disabled) { background:#166534; }
.btn-ghost { background:transparent; color:#2563eb; }
.btn-ghost:hover:not(:disabled) { background:#eff6ff; }
.btn-ghost:active:not(:disabled) { background:#dbeafe; }
.btn-sm { padding:6px 10px; font-size:13px; }
.week-label { font-weight:600; padding:6px 8px; display:inline-block; }

/* ===== 表单 ===== */
.field { display:flex; flex-direction:column; gap:6px; margin-bottom:14px; }
.field label { font-size:13px; color:#6b7280; font-weight:500; }
.field .required::after { content:' *'; color:#dc2626; }
.field .hint { font-size:12px; color:#9ca3af; }
.field .field-error { font-size:12px; color:#dc2626; display:none; }
.field.is-error .field-error { display:block; }
.field.is-error .input, .field.is-error .select { border-color:#dc2626; }
.input, .select { width:100%; padding:8px 10px; border:1px solid #d1d5db; border-radius:6px;
  font-size:14px; color:#222; background:#fff; font-family:inherit; }
.input::placeholder { color:#9ca3af; }
.input:focus, .select:focus { outline:none; border-color:#2563eb; box-shadow:0 0 0 3px rgba(37,99,235,.18); }
.input:disabled, .select:disabled { background:#f3f4f6; color:#9ca3af; cursor:not-allowed; }

/* ===== 表格 ===== */
.table { width:100%; border-collapse:collapse; font-size:14px; }
.table th { text-align:left; padding:8px 10px; border-bottom:2px solid #e5e7eb; white-space:nowrap;
  color:#6b7280; font-weight:600; background:#fafafa; }
.table td { padding:8px 10px; border-bottom:1px solid #f0f0f0; vertical-align:top; }
.table tbody tr:hover { background:#f8fafc; }

/* ===== 弹窗 ===== */
.modal-overlay { position:fixed; inset:0; background:rgba(0,0,0,.4); display:flex; align-items:center;
  justify-content:center; z-index:1000; animation:fadeIn .15s ease; }
.modal-box { background:#fff; border-radius:10px; max-width:640px; width:92%; max-height:85vh;
  display:flex; flex-direction:column; box-shadow:0 4px 12px rgba(0,0,0,.12); animation:modalIn .18s ease; }
.modal-header { padding:14px 18px; font-weight:600; border-bottom:1px solid #eee; }
.modal-body { padding:16px 18px; overflow-y:auto; }
.modal-footer { padding:12px 18px; border-top:1px solid #eee; display:flex; justify-content:flex-end; gap:8px; }
@keyframes fadeIn { from { opacity:0; } to { opacity:1; } }
@keyframes modalIn { from { opacity:0; transform:translateY(-8px); } to { opacity:1; transform:none; } }

/* ===== toast ===== */
.toast-container { position:fixed; top:16px; right:16px; z-index:9999; display:flex; flex-direction:column; gap:8px; }
.toast { background:#fff; border:1px solid #e5e7eb; color:#222; padding:10px 14px; border-radius:6px;
  box-shadow:0 2px 6px rgba(0,0,0,.1); font-size:14px; display:flex; align-items:center; gap:8px;
  animation:toastIn .2s ease; }
.toast-error { border-color:#dc2626; color:#dc2626; }
.toast-success { border-color:#16a34a; color:#16a34a; }
.toast-info { border-color:#2563eb; color:#2563eb; }
@keyframes toastIn { from { opacity:0; transform:translateY(-8px); } to { opacity:1; transform:none; } }

/* ===== 导航 ===== */
.nav-tabs { display:flex; gap:8px; margin-bottom:16px; }

/* ===== 周历 ===== */
.cal-grid { display:grid; grid-template-columns:70px repeat(7,1fr); gap:6px; font-size:13px; }
.cal-corner { font-weight:600; text-align:center; padding:4px; }
.cal-cell { min-height:90px; border:1px solid #e5e7eb; border-radius:6px; padding:6px; background:#fafafa; }
.cal-cell.empty { color:#9ca3af; text-align:center; cursor:pointer; display:flex; align-items:center;
  justify-content:center; font-size:12px; }
.cal-cell.empty:hover { background:#f3f4f6; color:#6b7280; }
.drop-target { outline:2px solid #2563eb; outline-offset:-2px; background:#eff6ff !important; }
.sch-card { border:1px solid #dbeafe; background:#eff6ff; border-radius:6px; padding:6px; margin-bottom:6px;
  transition:box-shadow .15s; }
.sch-card:hover { box-shadow:0 1px 3px rgba(0,0,0,.1); }
.sch-title { font-weight:600; }
.staff-chip { display:inline-block; background:#fff; border:1px solid #bfdbfe; border-radius:10px;
  padding:1px 8px; margin:2px; cursor:grab; transition:background-color .15s; }
.staff-chip:hover { background:#eff6ff; }
`;

export function injectGlobalStyles() {
  if (document.getElementById('app-theme')) return;
  const style = document.createElement('style');
  style.id = 'app-theme';
  style.textContent = css;
  document.head.appendChild(style);
}
```

- [ ] **Step 2: main.js 顶部注入**

在 `src/main.js` 第 1 行之前插入：

```js
import { injectGlobalStyles } from './ui/theme.js';
injectGlobalStyles();
```

- [ ] **Step 3: 手测验证注入**

Run: `npm run dev`，浏览器打开 dev server（默认 http://localhost:5173）
Expected: 页面正常渲染；DevTools 中 `<head>` 存在 `#app-theme` `<style>` 且含 `.btn`/`.modal-box` 等类；console 无报错。页面外观暂与迁移前一致（视图尚未使用这些类，属预期）。

- [ ] **Step 4: Commit**

```bash
git add src/ui/theme.js src/main.js
git commit -m "[UI] 主题系统：设计令牌 + 全局样式注入"
```

---

### Task 2: 弹窗收口 modal.js

**Files:**
- Modify: `src/ui/modal.js`（整文件重写）

**Interfaces:**
- Consumes: theme.js 注入的 `.modal-*` 类
- Produces: 保持 `openModal({ title, body, footer }) => { close }` 签名不变，所有调用方（config/calendar）无感

- [ ] **Step 1: 重写 modal.js（完整内容）**

```js
export function openModal({ title, body, footer }) {
  const overlay = document.createElement('div');
  overlay.className = 'modal-overlay';
  const box = document.createElement('div');
  box.className = 'modal-box';
  const header = document.createElement('div');
  header.className = 'modal-header';
  header.textContent = title;
  const bodyEl = document.createElement('div');
  bodyEl.className = 'modal-body';
  bodyEl.append(body);
  const footerEl = document.createElement('div');
  footerEl.className = 'modal-footer';
  if (footer) footerEl.append(footer);
  const closeBtn = document.createElement('button');
  closeBtn.type = 'button';
  closeBtn.className = 'btn btn-default';
  closeBtn.textContent = '关闭';
  closeBtn.onclick = close;
  footerEl.appendChild(closeBtn);
  box.append(header, bodyEl, footerEl);
  overlay.appendChild(box);
  overlay.onclick = (e) => { if (e.target === overlay) close(); };
  document.body.appendChild(overlay);
  document.addEventListener('keydown', onKeydown);
  closeBtn.focus();

  function onKeydown(e) { if (e.key === 'Escape') close(); }
  function close() {
    document.removeEventListener('keydown', onKeydown);
    overlay.remove();
  }
  return { close };
}
```

- [ ] **Step 2: 手测弹窗**

Run: `npm run dev`，在基础配置页点"新增人员"
Expected: 弹窗出现，带淡入动画；footer 有"关闭"（白底灰边）按钮；按 Escape 关闭；点击遮罩空白处关闭；打开时焦点落在"关闭"按钮上；console 无报错。

- [ ] **Step 3: Commit**

```bash
git add src/ui/modal.js
git commit -m "[UI] 弹窗：接入主题类 + 淡入动画 + Escape/焦点管理"
```

---

### Task 3: toast 收口 toast.js

**Files:**
- Modify: `src/ui/toast.js`（整文件重写）

**Interfaces:**
- Produces: 保持 `showToast(msg, type) => void` 签名不变；type 支持 'info'/'success'/'error'（现有调用只用到这三种）
- Consumes: theme.js 注入的 `.toast-*` 类

- [ ] **Step 1: 重写 toast.js（完整内容）**

```js
const ICONS = { error: '✕', success: '✓', info: 'ℹ' };

export function showToast(msg, type = 'info') {
  let el = document.querySelector('.toast-container');
  if (!el) {
    el = document.createElement('div');
    el.className = 'toast-container';
    document.body.appendChild(el);
  }
  const t = document.createElement('div');
  t.className = `toast toast-${type}`;
  const icon = document.createElement('span');
  icon.textContent = ICONS[type] ?? 'ℹ';
  const text = document.createElement('span');
  text.textContent = msg;
  t.append(icon, text);
  el.appendChild(t);
  setTimeout(() => t.remove(), 2500);
}
```

- [ ] **Step 2: 手测 toast**

Run: `npm run dev`，在基础配置页保存一次（触发 success toast）、故意触发校验错误（触发 error toast）
Expected: 右上角出现带图标与边框色的 toast，有淡入动画，2.5s 自动消失；error 红色 / success 绿色。

- [ ] **Step 3: Commit**

```bash
git add src/ui/toast.js
git commit -m "[UI] toast：接入主题类 + 图标 + 淡入动画"
```

---

### Task 4: model.js 校验结构化 + 测试同步

**Files:**
- Modify: `src/data/model.js:50-77`
- Modify: `test/model.test.js:15-24`（2 处断言）
- Modify: `src/views/config.js:106,181`（2 处 `v.errors.join('；')` → 兼容临时写法）

**Interfaces:**
- Produces: `validateStaff/validateProject` 返回 `{ valid, errors }`，其中 `errors` 为 `[{ field, msg }]` 数组。field 取值：Project 用 `name/fatigueScore/requiredCapacity/weekDays/slots`；Staff 用 `name/status/maxWeeklyFatigue/maxHeavyTaskCount/bannedProjects/preferredProjects`
- Consumes: 无（纯模型层改动）

> 注：本任务只把返回结构改为对象数组，config.js 暂用 `.map(e=>e.msg).join('；')` 维持 toast 行为（Task 5 再行内化），保证此步完成后 UI 不回归。

- [ ] **Step 1: 先改 model.test.js 断言（红）**

`test/model.test.js` 中把 `r.errors.some(e => e.includes(...))` 改为 `r.errors.some(e => e.msg.includes(...))`：

```js
// 第 15-20 行 test('validateProject: 非法劳累指数') 内：
assert.ok(r.errors.some(e => e.msg.includes('劳累')));

// 第 22 行起 test('validateProject: 空 slot 标签非法') 内：
// 原为 assert.ok(r.errors.some(e => e.includes('标签'))) 的，改为：
assert.ok(r.errors.some(e => e.msg.includes('标签')));
```

（如第二个测试的断言措辞不同，以 `e.msg.includes('标签')` 语义为准；改完先读一遍确认两处都覆盖。）

- [ ] **Step 2: 跑测试确认失败**

Run: `npm test`
Expected: model.test.js 上述用例 FAIL（errors 仍是 string，`e.msg` 为 undefined）

- [ ] **Step 3: 改 model.js 的 problems/validate（绿）**

将 `src/data/model.js` 的 `problems` 函数与两个 `validate` 的每条 check 补上 `field`：

```js
function problems(checks) {
  return checks.filter(c => c.cond).map(c => ({ field: c.field, msg: c.msg }));
}

export function validateProject(p) {
  const errors = problems([
    { cond: !p.name, field: 'name', msg: '任务名不能为空' },
    { cond: p.fatigueScore < 1 || p.fatigueScore > FATIGUE_MAX, field: 'fatigueScore', msg: '劳累指数必须为 1-3' },
    { cond: p.requiredCapacity < 1, field: 'requiredCapacity', msg: '所需人数必须 >= 1' },
    { cond: p.weekDays.some(d => d < 0 || d > 6), field: 'weekDays', msg: '重复星期必须为 0-6' },
    { cond: p.slots.some(s => !SLOT_LABELS.includes(s.label)), field: 'slots', msg: '时段标签必须在预置集合内' },
    { cond: p.slots.some(s => !s.startTime || !s.endTime), field: 'slots', msg: '时段必须包含起止时间' },
  ]);
  return { valid: errors.length === 0, errors };
}

export function validateStaff(s) {
  const errors = problems([
    { cond: !s.name, field: 'name', msg: '姓名不能为空' },
    { cond: !STAFF_STATUSES.includes(s.status), field: 'status', msg: '状态必须为 new/active/left' },
    { cond: s.maxWeeklyFatigue < 1, field: 'maxWeeklyFatigue', msg: '周疲劳上限必须 >= 1' },
    { cond: s.maxHeavyTaskCount < 0, field: 'maxHeavyTaskCount', msg: '高强度次数上限必须 >= 0' },
    { cond: s.bannedProjects.some(b => !b.projectId), field: 'bannedProjects', msg: '禁忌项目必须包含 projectId' },
    { cond: s.preferredProjects.some(p => !p.projectId), field: 'preferredProjects', msg: '擅长项目必须包含 projectId' },
  ]);
  return { valid: errors.length === 0, errors };
}
```

- [ ] **Step 4: 同步 config.js 两处调用（临时保持 toast 行为）**

`src/views/config.js:106` 与 `:181`，把：

```js
showToast(v.errors.join('；'), 'error');
```

改为：

```js
showToast(v.errors.map(e => e.msg).join('；'), 'error');
```

- [ ] **Step 5: 跑全量测试确认绿**

Run: `npm test`
Expected: 全部通过（expand/filter/model/score/substitute/week/db 各测试 PASS）

- [ ] **Step 6: Commit**

```bash
git add src/data/model.js src/views/config.js test/model.test.js
git commit -m "[模型] 校验返回结构化 {field,msg}，为行内错误提示铺路"
```

---

### Task 5: fields.js 表单构建 + 编辑弹窗结构化

**Files:**
- Create: `src/ui/fields.js`
- Modify: `src/views/config.js:75-112`（editStaffDialog 重写）、`src/views/config.js:153-186`（editProjectDialog 重写）

**Interfaces:**
- Consumes: `openModal`（Task 2）、`field()`/`setError()`/`rowsEditor()`（本任务新增）、`tokens` 中对应类
- Produces: `field({ label, required, hint, control }) => { wrap, err }`；`setError(entry, msg)`（msg 空则清除）；`rowsEditor({ label, addLabel, cols, initial }) => { el, collect() }`。`cols` 项形如 `{ key, type: 'select'|'text'|'time', options?, placeholder? }`；`collect()` 返回对象数组，字段顺序与 `cols` 一致

- [ ] **Step 1: 创建 fields.js（完整内容）**

```js
export function field({ label, required = false, hint, control }) {
  const wrap = document.createElement('div');
  wrap.className = 'field';
  const lab = document.createElement('label');
  if (required) lab.classList.add('required');
  lab.textContent = label;
  wrap.appendChild(lab);
  wrap.appendChild(control);
  if (hint) {
    const h = document.createElement('div');
    h.className = 'hint';
    h.textContent = hint;
    wrap.appendChild(h);
  }
  const err = document.createElement('div');
  err.className = 'field-error';
  wrap.appendChild(err);
  return { wrap, err };
}

export function setError(entry, msg) {
  if (msg) {
    entry.wrap.classList.add('is-error');
    entry.err.textContent = msg;
  } else {
    entry.wrap.classList.remove('is-error');
    entry.err.textContent = '';
  }
}

export function rowsEditor({ label, addLabel, cols, initial = [] }) {
  const box = document.createElement('div');
  box.className = 'field';
  const lab = document.createElement('label');
  lab.textContent = label;
  const rows = document.createElement('div');
  rows.style.cssText = 'display:flex;flex-direction:column;gap:8px;';
  const add = document.createElement('button');
  add.type = 'button';
  add.className = 'btn btn-default btn-sm';
  add.textContent = addLabel;
  add.onclick = () => rows.appendChild(buildRow({}));
  box.append(lab, rows, add);

  function buildRow(data) {
    const row = document.createElement('div');
    row.style.cssText = 'display:flex;gap:8px;align-items:center;';
    for (const col of cols) {
      let el;
      if (col.type === 'select') {
        el = document.createElement('select');
        el.className = 'select';
        for (const opt of col.options) {
          const o = document.createElement('option');
          o.value = opt.value;
          o.textContent = opt.label;
          if (String(data[col.key]) === String(opt.value)) o.selected = true;
          el.appendChild(o);
        }
      } else {
        el = document.createElement('input');
        el.className = 'input';
        el.type = col.type ?? 'text';
        el.placeholder = col.placeholder ?? '';
        el.value = data[col.key] ?? '';
      }
      el.style.flex = '1';
      row.appendChild(el);
    }
    const del = document.createElement('button');
    del.type = 'button';
    del.className = 'btn btn-ghost btn-sm';
    del.textContent = '×';
    del.onclick = () => row.remove();
    row.appendChild(del);
    return row;
  }

  initial.forEach(d => rows.appendChild(buildRow(d)));

  return {
    el: box,
    collect() {
      const out = [];
      for (const row of rows.children) {
        if (row.tagName !== 'DIV') continue;
        const item = {};
        cols.forEach((col, i) => { item[col.key] = row.children[i].value; });
        out.push(item);
      }
      return out;
    },
  };
}
```

- [ ] **Step 2: 重写 editStaffDialog（结构化表单 + 行内错误）**

`src/views/config.js` 中把 `editStaffDialog` 整体替换为：

```js
async function editStaffDialog(staff) {
  const target = staff ?? createStaff({});
  const { projects } = await loadAll();
  const projectOptions = projects.map(p => ({ value: p.id, label: p.name }));
  const body = document.createElement('div');

  const nameInput = document.createElement('input');
  nameInput.className = 'input';
  nameInput.value = target.name;
  nameInput.placeholder = '请输入姓名';
  const nameF = field({ label: '姓名', required: true, control: nameInput });

  const statusSel = document.createElement('select');
  statusSel.className = 'select';
  for (const s of STAFF_STATUSES) {
    const o = document.createElement('option');
    o.value = s; o.textContent = s;
    if (s === target.status) o.selected = true;
    statusSel.appendChild(o);
  }
  const statusF = field({ label: '状态', control: statusSel, hint: 'new=新入保护，left=退出保留历史' });

  const allowedSel = document.createElement('select');
  allowedSel.className = 'select';
  allowedSel.multiple = true;
  for (const p of projects) {
    const o = document.createElement('option');
    o.value = p.id; o.textContent = p.name;
    if (target.allowedProjects.includes(p.id)) o.selected = true;
    allowedSel.appendChild(o);
  }
  const allowedF = field({ label: '可胜任项目', control: allowedSel, hint: '按住 Ctrl 可多选' });

  const preferredEditor = rowsEditor({
    label: '擅长项目（加分）', addLabel: '＋ 添加擅长项目',
    cols: [
      { key: 'projectId', type: 'select', options: projectOptions },
      { key: 'reason', type: 'text', placeholder: '如：体力好，搬运熟练' },
    ],
    initial: target.preferredProjects,
  });

  const bannedEditor = rowsEditor({
    label: '禁忌项目（硬性过滤）', addLabel: '＋ 添加禁忌项目',
    cols: [
      { key: 'projectId', type: 'select', options: projectOptions },
      { key: 'reason', type: 'text', placeholder: '如：腰伤，不宜搬重物' },
    ],
    initial: target.bannedProjects,
  });

  const fatigueInput = document.createElement('input');
  fatigueInput.className = 'input';
  fatigueInput.type = 'number';
  fatigueInput.min = 1;
  fatigueInput.value = target.maxWeeklyFatigue;
  const fatigueF = field({ label: '周疲劳上限', control: fatigueInput });

  const heavyInput = document.createElement('input');
  heavyInput.className = 'input';
  heavyInput.type = 'number';
  heavyInput.min = 0;
  heavyInput.value = target.maxHeavyTaskCount;
  const heavyF = field({ label: '高强度次数上限', control: heavyInput });

  body.append(nameF.wrap, statusF.wrap, allowedF.wrap, preferredEditor.el, bannedEditor.el, fatigueF.wrap, heavyF.wrap);
  const footer = document.createElement('div');
  const saveBtn = document.createElement('button');
  saveBtn.type = 'button';
  saveBtn.className = 'btn btn-primary';
  saveBtn.textContent = '保存';
  footer.appendChild(saveBtn);
  const modal = openModal({ title: staff ? '编辑人员' : '新增人员', body, footer });

  saveBtn.onclick = async () => {
    const draft = createStaff({
      id: target.id,
      name: nameInput.value,
      status: statusSel.value,
      allowedProjects: [...allowedSel.selectedOptions].map(o => o.value),
      preferredProjects: preferredEditor.collect(),
      bannedProjects: bannedEditor.collect(),
      maxWeeklyFatigue: Number(fatigueInput.value),
      maxHeavyTaskCount: Number(heavyInput.value),
    });
    const v = validateStaff(draft);
    if (!v.valid) {
      const byField = {};
      v.errors.forEach(e => { (byField[e.field] ??= []).push(e.msg); });
      setError(nameF, byField.name?.join('；') || '');
      setError(fatigueF, byField.maxWeeklyFatigue?.join('；') || '');
      setError(heavyF, byField.maxHeavyTaskCount?.join('；') || '');
      if (byField.bannedProjects) showToast(byField.bannedProjects.join('；'), 'error');
      if (byField.preferredProjects) showToast(byField.preferredProjects.join('；'), 'error');
      return;
    }
    await saveStaff(draft);
    modal.close();
    showToast('已保存', 'success');
    renderConfig(document.querySelector('#view'));
  };
}
```

同时 `config.js` 顶部 import 增加：

```js
import { field, setError, rowsEditor } from '../ui/fields.js';
```

- [ ] **Step 3: 重写 editProjectDialog**

`src/views/config.js` 中把 `editProjectDialog` 整体替换为：

```js
async function editProjectDialog(project) {
  const target = project ?? createProject({});
  const body = document.createElement('div');

  const nameInput = document.createElement('input');
  nameInput.className = 'input';
  nameInput.value = target.name;
  nameInput.placeholder = '请输入任务名';
  const nameF = field({ label: '名称', required: true, control: nameInput });

  const fatigueSel = document.createElement('select');
  fatigueSel.className = 'select';
  for (let i = 1; i <= FATIGUE_MAX; i++) {
    const o = document.createElement('option');
    o.value = i; o.textContent = `${i}（${i === 1 ? '轻松' : i === 2 ? '中等' : '高强度'}）`;
    if (target.fatigueScore === i) o.selected = true;
    fatigueSel.appendChild(o);
  }
  const fatigueF = field({ label: '劳累指数', control: fatigueSel });

  const capInput = document.createElement('input');
  capInput.className = 'input';
  capInput.type = 'number';
  capInput.min = 1;
  capInput.value = target.requiredCapacity;
  const capF = field({ label: '所需人数', control: capInput });

  const daysWrap = document.createElement('div');
  daysWrap.style.cssText = 'display:flex;gap:6px;flex-wrap:wrap;';
  const dayChecks = [];
  const DAYS = ['日', '一', '二', '三', '四', '五', '六'];
  for (let d = 0; d < 7; d++) {
    const lab = document.createElement('label');
    lab.style.cssText = 'display:inline-flex;align-items:center;gap:4px;font-size:13px;padding:4px 8px;border:1px solid #d1d5db;border-radius:6px;cursor:pointer;';
    const cb = document.createElement('input');
    cb.type = 'checkbox';
    cb.value = d;
    cb.checked = target.weekDays.includes(d);
    dayChecks.push(cb);
    lab.append(cb, document.createTextNode(`周${DAYS[d]}`));
    daysWrap.appendChild(lab);
  }
  const daysF = field({ label: '重复星期（全不勾 = 一次性任务）', control: daysWrap });

  const slotEditor = rowsEditor({
    label: '时段', addLabel: '＋ 添加时段',
    cols: [
      { key: 'label', type: 'select', options: SLOT_LABELS.map(s => ({ value: s, label: s })) },
      { key: 'startTime', type: 'time' },
      { key: 'endTime', type: 'time' },
    ],
    initial: target.slots.map(s => ({ label: s.label, startTime: s.startTime, endTime: s.endTime })),
  });

  const activeSel = document.createElement('select');
  activeSel.className = 'select';
  for (const [val, txt] of [['true', '启用'], ['false', '停用']]) {
    const o = document.createElement('option');
    o.value = val; o.textContent = txt;
    if (String(target.active) === val) o.selected = true;
    activeSel.appendChild(o);
  }
  const activeF = field({ label: '启用状态', control: activeSel });

  body.append(nameF.wrap, fatigueF.wrap, capF.wrap, daysF.wrap, slotEditor.el, activeF.wrap);
  const footer = document.createElement('div');
  const saveBtn = document.createElement('button');
  saveBtn.type = 'button';
  saveBtn.className = 'btn btn-primary';
  saveBtn.textContent = '保存';
  footer.appendChild(saveBtn);
  const modal = openModal({ title: project ? '编辑任务' : '新增任务', body, footer });

  saveBtn.onclick = async () => {
    const draft = createProject({
      id: target.id,
      name: nameInput.value,
      fatigueScore: Number(fatigueSel.value),
      requiredCapacity: Number(capInput.value),
      weekDays: dayChecks.filter(c => c.checked).map(c => Number(c.value)),
      slots: slotEditor.collect(),
      active: activeSel.value === 'true',
    });
    const v = validateProject(draft);
    if (!v.valid) {
      const byField = {};
      v.errors.forEach(e => { (byField[e.field] ??= []).push(e.msg); });
      setError(nameF, byField.name?.join('；') || '');
      setError(fatigueF, byField.fatigueScore?.join('；') || '');
      setError(capF, byField.requiredCapacity?.join('；') || '');
      if (byField.slots) showToast(byField.slots.join('；'), 'error');
      if (byField.weekDays) showToast(byField.weekDays.join('；'), 'error');
      return;
    }
    await saveProject(draft);
    modal.close();
    showToast('已保存', 'success');
    renderConfig(document.querySelector('#view'));
  };
}
```

注意：`editStaffDialog`/`editProjectDialog` 的调用点（`actions.children[0].onclick = () => editStaffDialog()` 等）无需改，仍是零参调用（async 自动执行）。两函数定义位置保持在原处（renderStaffs/renderProjects 之后）。

- [ ] **Step 4: 手测结构化表单**

Run: `npm run dev`，进入基础配置 → 任务管理
Expected:
- 新增任务：名称必填、劳累指数下拉(1-3 带中文说明)、人数数字、重复星期 7 个 chips 可多选、时段行"标签+开始+结束+×"可增删（时间为原生 time 选择器）、启用下拉
- 故意清空名称保存 → 名称下方出现红色行内错误"任务名不能为空"，弹窗不关闭
- 新增人员：可胜任项目可 Ctrl 多选、擅长/禁忌行为"项目下拉+原因+×"可增删、上下限数字
- 编辑既有数据 → 下拉/多选/chips/动态行正确回显（含擅长的原因、时段起止时间）
- 保存成功 → 绿色 toast + 表格刷新

- [ ] **Step 5: Commit**

```bash
git add src/ui/fields.js src/views/config.js
git commit -m "[UI] 表单：结构化动态行编辑器 + 编辑弹窗告别 JSON 输入 + 错误行内提示"
```

---

### Task 6: config 表格与按钮迁移

**Files:**
- Modify: `src/views/config.js`（btn helper、renderStaffs/renderProjects 的 actions/表格/td/编辑按钮）

**Interfaces:**
- Consumes: theme.js 注入的 `.table`/`.btn`/`.btn-sm`/`.btn-default`/`.btn-primary` 类
- Produces: 无（内部重构）

- [ ] **Step 1: 替换 btn helper 与 setActive**

把 `src/views/config.js` 的 `btn` 函数替换为：

```js
function btn(text, active = false) {
  const b = document.createElement('button');
  b.type = 'button';
  b.textContent = text;
  b.className = `btn btn-${active ? 'primary' : 'default'}`;
  return b;
}
```

删除 `setActive` 函数；tab 切换处（`renderConfig` 内 `tabStaff.onclick`/`tabProject.onclick`）改用换类：

```js
tabStaff.onclick = () => { setTab(tabStaff, tabProject); renderStaffs(body); };
tabProject.onclick = () => { setTab(tabProject, tabStaff); renderProjects(body); };
```

并在文件内新增：

```js
function setTab(on, off) {
  on.className = 'btn btn-primary';
  off.className = 'btn btn-default';
}
```

- [ ] **Step 2: 表格与操作按钮迁移**

`renderStaffs` 与 `renderProjects` 内：
- `table.style.cssText = 'width:100%;border-collapse:collapse;font-size:14px;'` → `table.className = 'table'`
- `<thead><tr>...<th style="text-align:left;padding:8px;border-bottom:2px solid #ddd;white-space:nowrap;">` → `<thead><tr>...<th>`（类已含 th 样式）
- 每行 `<td style="padding:8px;border-bottom:1px solid #eee;">` → `<td>`
- 操作列 `<td style="padding:8px;"><button data-edit>编辑</button></td>` → `<td><button type="button" data-edit class="btn btn-default btn-sm">编辑</button></td>`

`actions` 容器（`display:flex;gap:8px;margin-bottom:10px;`）保持内联（仅布局，非样式令牌）可不动；其内按钮已由 `btn()` 生成 `.btn` 类。

- [ ] **Step 3: 手测配置页**

Run: `npm run dev`，基础配置 → 人员/任务
Expected: 顶部 tab 选中为蓝底白字、未选为白底灰边；工具按钮（新增/Excel/JSON）样式统一，hover 有反馈；表格表头浅灰底、行 hover 高亮、"编辑"为紧凑按钮；无任何内联残留按钮样式。

- [ ] **Step 4: Commit**

```bash
git add src/views/config.js
git commit -m "[UI] 配置页：表格/按钮接入主题类，tab 切换改类"
```

---

### Task 7: 周历视图迁移 calendar.js

**Files:**
- Modify: `src/views/calendar.js`

**Interfaces:**
- Consumes: theme.js 注入的 `.btn*`/`.cal-*`/`.sch-*`/`.staff-chip`/`.drop-target`/`.input`/`.select` 类；`field()`（手动建班次弹窗）
- Produces: 无（内部重构）；`enableDrop` 的 `drop-target` 高亮由 dnd.js 原样保留

- [ ] **Step 1: 替换 btn helper**

```js
function btn(text, active = false, plain = false) {
  const b = document.createElement('button');
  b.type = 'button';
  b.textContent = text;
  b.className = plain ? 'week-label' : `btn btn-${active ? 'primary' : 'default'}`;
  return b;
}
```

顶部工具条：`label = btn(getWeekLabel(currentWeekStart), false, true)` 现走 plain → 渲染为 `.week-label`，删除其后手动设置背景/边框的 4 行（`label.style.background='none'` 等）。

- [ ] **Step 2: 周历网格迁移**

`renderCalendar` 内：
- `grid.style.cssText = 'display:grid;...'` → `grid.className = 'cal-grid'`
- `grid.appendChild(corner('时段'))` → `grid.appendChild(corner('时段'))` 不变；`corner()` 内 `c.style.cssText = 'font-weight:600;text-align:center;padding:4px;'` → `c.className = 'cal-corner'`
- 单元格：`cell.style.cssText = 'min-height:90px;...'` → `cell.className = 'cal-cell'`
- 空单元格（当前小"+"）：改为提示文本，挂 `.cal-cell.empty`：

```js
if (cellSchedules.length === 0) {
  const empty = document.createElement('div');
  empty.className = 'cal-cell empty';
  empty.textContent = '＋ 手动建班次';
  empty.onclick = () => manualCreate(date, slotLabel);
  cell.appendChild(empty);
}
```

- [ ] **Step 3: 班次卡片与 chips 迁移**

`renderScheduleCard` 内：
- `card.style.cssText = 'border:1px solid #dbeafe;...'` → `card.className = 'sch-card'`
- `title.style.fontWeight = '600'` → `title.className = 'sch-title'`
- `chip.style.cssText = 'display:inline-block;...'` → `chip.className = 'staff-chip'`

- [ ] **Step 4: 弹窗与按钮迁移**

- `staffDialog` 的 `leaveBtn`：`leaveBtn.style.cssText = 'padding:8px 14px;background:#dc2626;...'` → `leaveBtn.className = 'btn btn-danger'`（补 `leaveBtn.type='button'`）
- `openSubstituteModal` 的 `pick` 按钮：`pick.style.cssText = 'margin-top:6px;padding:6px 12px;background:#16a34a;...'` → `pick.className = 'btn btn-success btn-sm'`；`row.style.cssText = 'border:1px solid #eee;...'` 保持内联（行卡片，非令牌）可不动
- `manualCreate` 弹窗：三个控件 `date` 输入 / `slot` 选择 / `project` 选择分别加 `className`，`date` 用 `input.className='input'`，两个 select 用 `select.className='select'`；`okBtn` → `okBtn.className='btn btn-primary'`（补 `type='button'`）
- 顶部 `smartFill` 自动填充：无样式改动

- [ ] **Step 5: 手测周历全流程**

Run: `npm run dev`，排班周历
Expected: 周切换/智能排班/手动建班次按钮样式统一有 hover；周标签为加粗文本；网格四行七列对齐正常；空单元格显示"＋ 手动建班次"浅色提示、hover 变深；班次卡片有 hover 投影；人员 chip hover 变蓝底、仍可拖拽；手动建班次弹窗控件为新样式；**拖拽人名到非法目标仍被硬性过滤拦截并 toast 原因**（回归保护）；请假替补弹窗的"标记请假"红底、"选此替补"绿底。

- [ ] **Step 6: Commit**

```bash
git add src/views/calendar.js
git commit -m "[UI] 周历：按钮/卡片/chips/空状态接入主题类"
```

---

### Task 8: 疲劳分析迁移 analysis.js

**Files:**
- Modify: `src/views/analysis.js:15-24`

**Interfaces:**
- Consumes: theme.js 注入的 `.btn`/`.btn-default`/`.week-label` 类
- Produces: 无（内部重构）

- [ ] **Step 1: 替换顶部按钮**

`src/views/analysis.js` 的 `renderAnalysis` 内：

```js
const prev = document.createElement('button'), next = document.createElement('button');
prev.textContent = '← 上周'; next.textContent = '下周 →';
```

改为：

```js
const prev = document.createElement('button'), next = document.createElement('button');
prev.type = 'button'; next.type = 'button';
prev.className = 'btn btn-default'; next.className = 'btn btn-default';
prev.textContent = '← 上周'; next.textContent = '下周 →';
```

`label` 已是 `<span>` 且 `label.style.fontWeight='600'`，改为 `label.className = 'week-label'`（删掉 `label.style.fontWeight='600'`）。

- [ ] **Step 2: 手测分析页**

Run: `npm run dev`，疲劳分析
Expected: ←上周/下周→ 为统一按钮有 hover；周范围文本加粗；canvas 柱状图渲染、切换周正常。

- [ ] **Step 3: Commit**

```bash
git add src/views/analysis.js
git commit -m "[UI] 分析：按钮/周标签接入主题类"
```

---

### Task 9: 导航装配 + 全量验证 + 打包

**Files:**
- Modify: `index.html:11-14`（#nav 样式移除、挂类）
- Modify: `src/main.js`（buildNav 用 .btn）

**Interfaces:**
- Consumes: theme.js 注入的 `.nav-tabs`/`.btn` 类
- Produces: 无

- [ ] **Step 1: index.html 精简 #nav 样式**

`index.html` 的 `<style>` 中删除这两行：

```html
#nav { display: flex; gap: 8px; margin-bottom: 16px; }
#nav button { padding: 8px 16px; border: 1px solid #ccc; background: #fff; border-radius: 6px; cursor: pointer; }
#nav button.active { background: #2563eb; color: #fff; border-color: #2563eb; }
```

并将 `<div id="nav">` 改为 `<div id="nav" class="nav-tabs">`。

- [ ] **Step 2: main.js buildNav 接入 .btn**

`src/main.js` 的 `buildNav` 内：

```js
b.textContent = views[key].label;
b.className = key === current ? 'active' : '';
```

改为：

```js
b.type = 'button';
b.textContent = views[key].label;
b.className = `btn ${key === current ? 'btn-primary' : 'btn-default'}`;
```

- [ ] **Step 3: 全量单测 + 全流程手测 + 打包**

Run:
```bash
npm test
npm run dev
```
Expected: 单测全绿（41 算法/模型 + db 测试）；dev server 下逐项验证：
1. 导航三 tab（排班周历/基础配置/疲劳分析）样式统一，当前页蓝底
2. 三个视图切换正常，无 console 报错
3. 基础配置：新增/编辑人员与任务（结构化表单、动态行、多选、chips、错误行内提示、toast）
4. 周历：拖拽人名被非法目标拦截、智能排班填充、手动建班次、请假替补弹窗红/绿按钮
5. 疲劳分析柱状图与超限标注

Run:
```bash
npm run build
```
Expected: 输出 `dist/index.html` 单文件；`file://` 双击打开，样式（按钮/表单/表格）与 http 下一致，周历渲染正常，无空白页。

- [ ] **Step 4: Commit**

```bash
git add index.html src/main.js
git commit -m "[装配] 导航接入主题类，全量手测 + 单文件打包验证"
```

---

## 自审记录

- **Spec 覆盖**：tokens/按钮体系（Task 1）、表单类+动态行编辑器（Task 5）、弹窗/toast（Task 2/3）、表格（Task 6）、周历卡片与 chips（Task 7）、导航（Task 9）、错误行内化（Task 4+5）、打包验证（Task 9）—— spec 各节均有落点。
- **占位符**：无 TBD/TODO；每个改动步骤均含完整代码。
- **类型一致性**：`field()/setError()/rowsEditor()` 签名在 Task 5 定义，仅 Task 5 消费；`validateStaff/validateProject` 新结构在 Task 4 定义、Task 5 按 `{field,msg}` 消费；`.btn`/`.btn-primary`/`.btn-danger`/`.btn-success`/`.cal-*`/`.sch-*`/`.staff-chip`/`.week-label` 类在 Task 1 CSS 中均已定义，后序任务按同名引用。
