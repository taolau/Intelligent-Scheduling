# 团体组织轻量化智能排班系统 — 技术规范（spec.md）

> 本项目最高技术判定基准。所有实现必须与此对齐。
> 文档性质：**探讨版 PRD 对焦定稿**，2026-08-24 与 Tao 逐项对齐确认。

---

## 0. 项目定位

针对非公司制团体组织，基于"领导统筹分配"模式，做一款**纯前端（无后端服务器）**的轻量化排班工具。目标：
- 替代人工记忆，自动校验技能冲突、规避个人禁忌。
- 引入"劳累指数"与疲劳积分算法，实现**防透支、劳逸均衡**。
- 突发请假/离岗时，支持**临时一键替补**。
- 所有决策给出**可解释的理由**（为什么推荐 / 为什么阻止）。

---

## 1. 技术栈决策

| 项 | 决策 |
| --- | --- |
| 框架 | **无框架**，纯原生 ES Modules + 标准 Web API |
| 构建 | **开发多模块 + 发布单文件**：开发用 Vite dev server（模块化）；发布用 esbuild 打包为 `dist/index.html` 单文件，双击即用 |
| 外部依赖 | 仅 SheetJS（XLSX），用于 Excel 导入导出 |
| 存储 | localStorage（本地持久化）；db 层内存 Map 索引（O(1) CRUD、首次访问才从 localStorage 加载）；store 层增量缓存为唯一真源（视图经 getCache() 读、saveXxx 增量更新，loadAll 仅初始化/导入/重置） |
| 日历 | **不引入 FullCalendar**，自建周历网格视图（贴合"按星期几重复"场景） |
| 拖拽 | HTML5 Drag and Drop API |
| 图表 | 原生 `<canvas>` 自绘柱状图（疲劳分析） |
| 弹窗 | 原生 `<dialog>` |
| UI 样式 | **设计令牌 + 全局样式注入**：`src/ui/theme.js` 导出 `tokens` 并注入全局类（按钮五态/表单/弹窗/表格/toast/周历），视图挂 class 而非内联 cssText。内联样式无法表达 `:hover`/`:focus`/`:active`/`:disabled` 伪类，类体系是交互反馈的前提；样式随 JS 内联进单文件，build.js 不改 |

**架构分层**：算法层（`core/`）为**纯函数、无 DOM 依赖**，独立可测；视图层只负责展示与交互。

---

## 2. 架构与模块划分

```
Intelligent-Scheduling/
  index.html              # 入口（发布时 = esbuild 打包产物）
  src/
    data/                 # 数据层
      db.js               #   localStorage 封装 + 模型定义
      store.js            #   业务数据访问（项目/人员/班次/请假）
    core/                 # 算法层（纯函数，无 DOM）
      expand.js           #   按 weekDays + slots 展开班次
      filter.js           #   硬性过滤（一票否决，返回被拒原因）
      score.js            #   打分推荐（返回得分构成）
      substitute.js       #   替补 Top3 推荐
    views/                # 视图层
      calendar.js         #   周历网格视图（排班主界面）
      config.js           #   基础配置页（人员/任务表）
      analysis.js         #   疲劳分析（柱状图）
    ui/                   # 通用 UI 组件
      modal.js            #   弹窗、替补推荐弹窗
      dnd.js              #   拖拽人名
      excel.js            #   Excel 导入导出（SheetJS）
      toast.js            #   提示
  build.js                # esbuild 打包脚本 → dist/index.html
```

---

## 3. 核心数据结构

### 3.1 Project（任务/项目）

| 字段 | 类型 | 说明 | 示例 |
| --- | --- | --- | --- |
| `id` | String | 唯一标识 | `"P101"` |
| `name` | String | 任务名称 | `"场地搬运"` |
| `fatigueScore` | Number | 劳累指数（1-3）：1=轻松，2=中等，3=高强度 | `3` |
| `requiredCapacity` | Number | 单班次所需人数 | `2` |
| `weekDays` | Array | **重复于星期几** [0-6]（0=周日）；**空数组 = 一次性任务**（不自动展开，由领导手动建班次） | `[0]`（周日） |
| `slots` | Array | **时段数组（至少一个）**（一个任务可有多个时段，每时段仅标签无时间） | `[{label:"自主安排"}]` / `[{label:"早"}]` |
| `active` | Boolean | 启停标记；`false` 时不自动展开班次 | `true` |

`slots[].label` 字段说明：**时段标签来自系统预置集合**：`自主安排 / 早 / 中 / 晚`（实现前可增删）。**任务至少配置一个时段**，**所有时段均无具体时间范围**（startTime/endTime 留空，现阶段不做精确时间）。`自主安排` = 系统只排人不排时、由本人自主定时间；`早 / 中 / 晚` 仅作一天的组织标签（一人一时段至多一个任务，见 4.2）。

### 3.2 Staff（人员）

| 字段 | 类型 | 说明 | 示例 |
| --- | --- | --- | --- |
| `id` | String | 唯一标识 | `"S001"` |
| `name` | String | 姓名 | `"张三"` |
| `allowedProjects` | Array | 可胜任项目 ID 列表 | `["P101","P102"]` |
| `preferredProjects` | Array | **擅长项目列表（带原因）** | `[{projectId:"P101", reason:"体力好，搬运熟练"}]` |
| `bannedProjects` | Array | **禁忌项目列表（带原因，硬性过滤）** | `[{projectId:"P103", reason:"腰伤，不宜搬重物"}]` |
| `maxWeeklyFatigue` | Number | 单周劳累积分上限（防透支） | `6` |
| `maxHeavyTaskCount` | Number | 单周高强度（3分）次数上限 | `1` |
| `status` | String | 人员状态机（见 3.3） | `"active"` |

### 3.3 Staff 状态机

```
new（新加入）→ active（活跃）→ left（退出，保留历史，永不参与）
```

| 状态 | 参与排班 | 均衡加分 | 高强度任务 | 历史保留 |
| --- | --- | --- | --- | --- |
| `new` | 参与 | **按团队平均计**（均衡加分为 0，不被驱动填坑） | **一票否决**（3分任务不参与） | 保留 |
| `active` | 参与 | 正常（`(团队平均 − 本人当前周积分) × 5`） | 受 `maxHeavyTaskCount` 约束 | 保留 |
| `left` | 永不参与 | 从团队平均中排除 | — | 保留（历史/统计/导出仍含） |

- **转正**：领导在配置页手动将 `new` → `active`，解除新入保护。无自动转正。
- **请假不设独立状态**：通过 `Leave` 记录表达（见 3.5）。请一天假的 `active` 人员仍是 active，只是当天被硬性过滤排除；请假的临时性不影响状态机。

### 3.4 Schedule（排班记录）

| 字段 | 类型 | 说明 | 示例 |
| --- | --- | --- | --- |
| `id` | String | 排班记录 ID | `"SCH_001"` |
| `date` | String | 日期 `YYYY-MM-DD` | `"2026-08-30"` |
| `projectId` | String | 对应任务 ID | `"P101"` |
| `slotLabel` | String | 时段标签（引用 `Project.slots[].label`） | `"早"` |
| `staffIds` | Array | 已分配人员 ID 列表 | `["S001","S002"]` |

> **无 `status` 字段**：排班改即生效，无草案/确认两态。

### 3.5 Leave（请假记录）

| 字段 | 类型 | 说明 | 示例 |
| --- | --- | --- | --- |
| `id` | String | 唯一标识 | `"LV_001"` |
| `staffId` | String | 请假人 ID | `"S001"` |
| `date` | String | 请假日期 `YYYY-MM-DD` | `"2026-08-30"` |
| `reason` | String | 原因（可选） | `"家中有事"` |

- 请假按"人 + 某天"记录，**连续多天 = 多条 `date` 记录**（不引入 dateRange，保持轻量）。
- 请假当天该人员视为不可参与（硬性过滤排除）。

---

## 4. 核心算法

### 4.1 展开（expand）

```
选中某周 → 遍历所有 active 项目 × 其 weekDays 命中该周的天
        → 按项目每个 slot 生成班次（date + projectId + slotLabel + staffIds=[]）
一次性任务（weekDays=[]）不自动展开，领导手动建班次。
```

### 4.2 硬性过滤（filter，一票否决）

对候选人员，符合任一条件**严禁进入候选池**，并**返回被拒原因**：

1. `bannedProjects` 包含当前项目（含录入原因）。
2. 不具备 `allowedProjects` 权限。
3. **当日任务数超限**：该人员当天已排班次数 + 1 将超过 `dailyTaskLimit`（默认 2）。
4. **时段任务数超限**：该人员同一时段（自主安排/早/中/晚）已排班次数 + 1 将超过 `slotTaskLimit`（默认 1）。
5. 加入后本周累计疲劳将超过 `maxWeeklyFatigue`。
6. 高强度项目（3分）且加入后本周高强度次数将超过 `maxHeavyTaskCount`。
7. **请假中**：当日有 `Leave` 记录。
8. **已退出**：状态 `left`。
9. **新入保护**：状态 `new` 且该项目 `fatigueScore === 3`。

### 4.3 打分（score）

对通过硬性过滤的候选：

$$Score = 擅长加分 + 均衡加分$$

- **擅长加分**：该项目在 `preferredProjects` 中 → **+15**（附录入原因）。
- **均衡加分**：`(团队平均周积分 − 该人员当前周积分) × 5`
  - **团队平均** = 全部 `active` 状态人员的周积分平均值（**排除 `left` 状态人员**；`new` 人员按团队平均取值参与计算，即其均衡加分为 0；请假不改变平均基数）。
  - 周积分 = 本周（周一~周日，**固定自然周**）该人员所有已排班次的 `fatigueScore` 之和。
（间隔保护扣分已随「具体时间」一并移除，负荷由 4.2 数量上限 + 周疲劳控制。）

### 4.4 智能排班（自动填充）

```
领导点击"智能排班"按钮（手动触发，不自动）：
1. 取当前周视图内所有空班次（staffIds=[]）。
2. 排序：先按日期，同一天固定时段（早/中/晚）优先、自主安排最后（自主安排不占时段，留到软约束捡漏）。
3. 逐个班次：硬性过滤 → 打分 → 分配得分最高者 → 立即更新该人周积分 → 下一班次。
   （逐个更新周积分，保证后续班次的均衡加分感知前面分配。）
```

### 4.5 替补推荐（substitute）

```
领导对某人某天标记请假（按"人+某天"整处理）：
1. 收集该人员当天全部班次。
2. 对每个班次：剔除该人员 → 硬性过滤（跳过请假人） → 打分 → 取 Top3。
3. 输出：弹窗逐班次展示 Top3 + 推荐理由 + 一键替换。
```

### 4.6 可解释性（Decision Reasoning）

算法返回**结构化原因**，而非仅结果：

- 过滤返回：`{ rejected: true, reasons: ["黑名单：腰伤，不宜搬重物", "本周疲劳将超限"] }`
- 打分返回：`{ staffId, score, breakdown: [{label:"擅长搬运+15", reason:"体力好"}, ...] }`
- 替补弹窗、智能排班、拖拽阻止，均展示原因明细。

---

## 5. 功能模块与 UI 交互

```
[基础配置页] ➔ [周历网格看板] ➔ [智能排班/拖拽调整] ➔ [请假替补弹窗] ➔ [疲劳分析/导出]
```

### 5.1 基础配置页（config）
- 人员表：表格内编辑全部字段，含 `preferredProjects`/`bannedProjects` 的**原因录入**、`status` 状态管理（转正、请假、退出）。
- 任务表：编辑项目、重复规则（星期几多选）、时段（仅标签：自主安排/早/中/晚）。工具栏含「设置」弹窗（改数量上限与预警阈值）。
- Excel 模板导入 / 导出（人员、任务）。
- 一键导出全部数据为 JSON 备份。

### 5.2 周历网格看板（calendar）
- **行 = 预置时段标签（自主安排/早/中/晚），列 = 周一~周日**，行固定、不随项目动态增减，保证多项目对齐；**自主安排行置顶且高度较小（0.55fr）**，早/中/晚均分剩余；卡片不显示时间。
- 单元格 = 该时段展开的班次卡片（任务名 + 已分配人名）。
- **手动建班次**：点击空白单元格 → 选择任务（默认列出一次性任务）→ 创建班次，用于临时/一次性任务。
- **批量铺排**：工具栏按钮 → 弹窗输入 N（1-4）周 → 对接下来 N 周按 active 项目 weekDays+slots 展开班次空壳（幂等去重、不分配人员），用于提前计划。
- 周切换器查看任意周。
- **拖拽人名**到其他班次：实时硬性校验，非法移动显示原因 + 红框警告并阻止。
- 空班次显示"+ 智能排班"入口（触发 4.4）。

### 5.3 请假与替补（modal）
- 点击人名 → "标记请假" → 弹窗列出该人当天全部班次 → 逐班次 Top3 + 推荐理由 + 一键替换。
- 替换后原班次人员更新，请假人当日记录保留在 `Leave`。

### 5.4 疲劳分析（analysis）
- 本周全员柱状图：累计劳累积分 / 高强度次数双指标，横向对比。
- 超限人员红色标注；点击人员可下钻查看其得分构成/被拒原因。

### 5.5 导入导出
- Excel：仅人员/任务基础配置导入（班次不导入）；**排班结果可导出为 `.xlsx` 考勤表**（按周/月，含日期、任务、时段、人员）。
- JSON：全量备份 / 恢复。

---

## 6. 关键参数（可配置）

| 参数 | 默认 | 说明 |
| --- | --- | --- |
| `dailyTaskLimit` | `2` | 一人一天任务数上限（达上限 +1 硬性拒绝） |
| `slotTaskLimit` | `1` | 一人一时段任务数上限（达上限 +1 硬性拒绝） |
| `warnDailyCount` | `2` | 预警阈值：当天任务数达该值 chip 变黄 + 排班 toast 提醒 |
| `preferredBonus` | `15` | 擅长加分 |
| `balanceFactor` | `5` | 均衡加分系数 |
| `heavyFatigueThreshold` | `3` | 高强度判定阈值 |

---

## 7. 变更记录

- 2026-08-26：时段体系重构（**去时间 + 数量上限预警**，实现完成；由上午/中午/下午/晚上演进而来）。①时段标签 = 自主安排/早/中/晚，**全部无具体时间**（移除 DEFAULT_TIMES/fillSlotTimes/timeRange 组件），任务至少一个时段、时段仅标签。②**数量上限替代时间冲突**：filter 新增「当日任务数 ≤ dailyTaskLimit(2) / 一时段任务数 ≤ slotTaskLimit(1)」硬性过滤（自主安排计入数量），移除 hasTimeOverlap 与间隔保护扣分。③**预警**：当天任务数达 warnDailyCount(2) → 人员 chip 变黄 + 智能排班 toast 提示。④settings 存 is_sched:settings，配置页「设置」弹窗可改三项。⑤周历 4 行：自主安排置顶且高度 0.55fr（较矮），早/中/晚均分剩余；卡片不显示时间。⑥智能排班先固定时段（早/中/晚）后自主安排。⑦旧数据清空重配（历史时段标签不兼容）。
- 2026-08-26：侧边栏修复。①收缩/展开左上角品牌区高度恒定（收缩态 `.side-brand` padding 对齐展开态 13px，消除 6px 跳动）。②消除刷新 FOUC 裸闪：首屏侧边栏整套样式内联进 index.html `<style>`，theme.js 仍注入全量作覆盖；**两处样式需同步维护**（改侧边栏样式时 index.html 首屏段与 theme.js 侧边栏段一致）。
- 2026-08-26：存储层写放大优化。①db.js 内存 Map（id→record）索引：put/remove O(1)、getAll 免 JSON.parse，新增 `writeAll` 一次整表替换。②store.js 增量缓存：saveXxx/removeXxx 只更新 cache 不再 loadAll；loadAll 仅初始化/导入/重置；importJSON 用 writeAll 批量（O(n²)→O(n)）。③数据流铁律：**cache 唯一真源**，视图统一 getCache() 读，main.js 入口 loadAll 一次。④测试：新增 store.test.js 9 例 + db writeAll 用例。**局限**：写放大（整表 stringify）仍在，需 B 档按周分桶消除；db 内存 Map 后多标签页不同步（单标签场景无碍）。
- 2026-08-26：品牌与视觉升级。①**品牌**：浏览器标签/左上角改「民数记 Numbers」，品牌区双层（主标题+紫色小字副标题）、蒜图标渐变圆角容器。②**色系**：蒜皮紫 → **莓果梅紫**（主 #5a1d78 / hover #48115f / active #380c4a / 浅底 #f7f1fa / 活跃底 #efe3f6 / 边框 #e0d2ef / 深边框 #c9b0e0 / 页面底 #f7f4f8），theme.js 全量替换、无旧值残留。③**UI 精致化**：阴影改双阴影并带紫偏置、卡片 hover 上浮、中性色（文字/边框/背景）全向紫偏置、聚焦外发光柔和至 .14、弹窗/下拉/toast 双阴影、遮罩改深紫黑。④**疲劳分析页**：canvas 缓冲 × devicePixelRatio + setTransform（解决高分屏模糊）、图表容器 flex:1 弹性填满（消除下方空白）、空态提示、网格线带紫。
- 2026-08-25：周历排班页大优化 + 全局紫色设计语言。①**时区铁律**：日期一律本地 `todayStr()`/`toDateStr()`，禁用 `toISOString().slice(0,10)`（UTC 偏移致周切换错乱/凌晨定位错周，全项目 5 处修复）。②**布局**：body `height:100vh + overflow:hidden`，内容在各滚动容器，周历 grid `minmax(0,1fr)` 均分填满视口消除空白。③周历信息增强：班次卡片显时段时间+劳累指数徽标+缺员/满员角标，人员 chip 疲劳状态色（超限红/接近橙/正常），今天列高亮，**批量铺排未来 N 周**（`core/expand.js` 新增 `expandWeeks`）。④select 下拉面板 `fixed` 视口定位防弹窗内裁剪。⑤色系蓝→**蒜皮紫**（主色 #7c3aed、浅紫 #f5f3ff/#ede9fe、紫边框 #ddd6fe/#c4b5fd）。⑥config/analysis 两页视觉统一（分段Tab/状态徽标/摘要卡/圆角渐变柱）。
- 2026-08-25：UI 四轮打磨。①侧边栏：顶部 tab 改左侧可收缩侧栏（浅色、内联 SVG 线框图标、收起成 56px 图标窄条、localStorage 持久化）。②新增 `src/ui/select.js` 自定义下拉组件：单选/多选统一（选项打勾/键盘导航/多选标签增删/点击外部关闭），替换全部原生 select。③按钮交互反馈：按压 scale、hover 投影、active 内阴影、圆角 8px。④表单元素统一：周几选择胶囊 `.day-chip`、输入框 hover/圆角、周历卡片 hover 上浮+虚线空单元格、表格斑马纹。设计语言定调：8px 圆角、主色 `#2563eb`、按压/悬停反馈。
- 2026-08-24：基于探讨版 PRD 与 Tao 逐项对齐，定稿本 spec。新增：时段级排班、周历网格、人员状态机（new/active/left）、原因解释机制（可解释排班）、请假记录表。
- 2026-08-24：存储层 IndexedDB → localStorage。原因：file:// 双击打开时 Chromium 的 IndexedDB.open 永不回调导致空白页；localStorage 在 file:// 下可读写且持久，实现「双击即用」。
- 2026-08-24：UI 打磨定稿（按钮+表单+组件一致化）。新增 `src/ui/theme.js`（设计令牌+全局样式注入）与 `src/ui/fields.js`（结构化表单），编辑弹窗告别裸 JSON 输入、校验错误行内化；`validateProject/validateStaff` 返回结构化 `{field,msg}`。
