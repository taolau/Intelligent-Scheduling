# 团体组织轻量化智能排班系统 — 技术规范（spec.md）

> 本项目最高技术判定基准。所有实现必须与此对齐。
> 文档性质：**探讨版 PRD 对焦定稿**，2026-08-24 与 Tao 逐项对齐确认。

---

## 0. 项目定位

针对非公司制团体组织，基于"领导统筹分配"模式，做一款**纯前端（无后端服务器）**的轻量化排班工具。目标：
- 替代人工记忆，自动校验技能冲突、规避个人禁忌。
- 引入"劳累指数"与疲劳积分算法，实现**防透支、劳逸均衡**。
- 突发请假/离岗时，支持**临时一键替补**。
- 所有决策给出**可解释的理由**（为什么推荐 / 为什么阻止）。

---

## 1. 技术栈决策

| 项 | 决策 |
| --- | --- |
| 框架 | **无框架**，纯原生 ES Modules + 标准 Web API |
| 构建 | **开发多模块 + 发布单文件**：开发用 Vite dev server（模块化）；发布用 esbuild 打包为 `dist/index.html` 单文件，双击即用 |
| 外部依赖 | 仅 SheetJS（XLSX），用于 Excel 导入导出 |
| 存储 | localStorage（本地持久化）；db 层内存 Map 索引（O(1) CRUD、首次访问才从 localStorage 加载）；store 层增量缓存为唯一真源（视图经 getCache() 读、saveXxx 增量更新，loadAll 仅初始化/导入/重置） |
| 日历 | **不引入 FullCalendar**，自建周历网格视图（贴合"按星期几重复"场景） |
| 拖拽 | HTML5 Drag and Drop API |
| 图表 | 原生 `<canvas>` 自绘柱状图（疲劳分析） |
| 弹窗 | 原生 `<dialog>` |
| UI 样式 | **设计令牌 + 全局样式注入**：`src/ui/theme.js` 导出 `tokens` 并注入全局类（按钮五态/表单/弹窗/表格/toast/周历），视图挂 class 而非内联 cssText。内联样式无法表达 `:hover`/`:focus`/`:active`/`:disabled` 伪类，类体系是交互反馈的前提；样式随 JS 内联进单文件，build.js 不改 |

**架构分层**：算法层（`core/`）为**纯函数、无 DOM 依赖**，独立可测；视图层只负责展示与交互。

---

## 2. 架构与模块划分

```
Intelligent-Scheduling/
  index.html              # 入口（发布时 = esbuild 打包产物）
  src/
    data/                 # 数据层
      db.js               #   localStorage 封装 + 模型定义
      store.js            #   业务数据访问（项目/人员/班次/请假）
    core/                 # 算法层（纯函数，无 DOM）
      expand.js           #   按 weekDays + slots 展开班次
      filter.js           #   硬性过滤（一票否决，返回被拒原因）
      score.js            #   打分推荐（返回得分构成）
      substitute.js       #   替补 Top3 推荐
    views/                # 视图层
      calendar.js         #   周历网格视图（排班主界面）
      config.js           #   基础配置页（人员/任务表）
      analysis.js         #   疲劳分析（柱状图）
    ui/                   # 通用 UI 组件
      modal.js            #   弹窗、替补推荐弹窗
      dnd.js              #   拖拽人名
      excel.js            #   Excel 导入导出（SheetJS）
      toast.js            #   提示
  build.js                # esbuild 打包脚本 → dist/index.html
```

---

## 3. 核心数据结构

### 3.1 Project（任务/项目）

| 字段 | 类型 | 说明 | 示例 |
| --- | --- | --- | --- |
| `id` | String | 唯一标识 | `"P101"` |
| `name` | String | 任务名称 | `"场地搬运"` |
| `fatigueScore` | Number | 劳累指数（1-3）：1=轻松，2=中等，3=高强度 | `3` |
| `requiredCapacity` | Number | 单班次所需人数 | `2` |
| `weekDays` | Array | **重复于星期几** [0-6]（0=周日）；**空数组 = 一次性任务**（不自动展开，由领导手动建班次） | `[0]`（周日） |
| `slots` | Array | **时段数组（至少一个）**（一个任务可有多个时段，每时段仅标签无时间） | `[{label:"自主安排"}]` / `[{label:"早"}]` |
| `active` | Boolean | 启停标记；`false` 时不自动展开班次 | `true` |
| `timeRange` | Object/null | **选填执行窗口** `{start,end}`（HH:mm，如 `{start:"08:00",end:"18:00"}`）；**仅展示**（配置卡/周历卡片小字），不参与 filter/score/expand，不做冲突检测；不填为 `null` | `null` |

`slots[].label` 字段说明：**时段标签来自系统预置集合**：`自主安排 / 早 / 中 / 晚`（实现前可增删）。**任务至少配置一个时段**；任务级可选填 `timeRange` 执行窗口（仅展示、不参与算法，见 3.1），时段本身仍无具体时间。`自主安排` = 系统只排人不排时、由本人自主定时间；`早 / 中 / 晚` 仅作一天的组织标签（一人一时段至多一个任务，见 4.2）。

### 3.2 Staff（人员）

| 字段 | 类型 | 说明 | 示例 |
| --- | --- | --- | --- |
| `id` | String | 唯一标识 | `"S001"` |
| `name` | String | 姓名 | `"张三"` |
| `allowedProjects` | Array | 可胜任项目 ID 列表 | `["P101","P102"]` |
| `preferredProjects` | Array | **擅长项目列表（带原因）** | `[{projectId:"P101", reason:"体力好，搬运熟练"}]` |
| `bannedProjects` | Array | **禁忌项目列表（带原因，硬性过滤）** | `[{projectId:"P103", reason:"腰伤，不宜搬重物"}]` |
| `maxWeeklyFatigue` | Number | 单周劳累积分上限（防透支） | `6` |
| `maxHeavyTaskCount` | Number | 单周高强度（3分）次数上限 | `1` |
| `status` | String | 人员状态机（见 3.3） | `"active"` |
| `joinedAt` | Number | 加入时间戳（ms），配置页卡片排序依据（新加入在前，已退出殿后） | `1785000000000` |
| `restFrom` | String/null | 休假前状态（`"new"`/`"active"`），开关恢复参与时还原；非休假状态为 `null` | `"active"` |

### 3.3 Staff 状态机

```
new（新加入）→ active（活跃）→ left（退出，保留历史，永不参与）
                     ↕（开关可逆）
                  rest（休假）
```

| 状态 | 参与排班 | 均衡加分 | 高强度任务 | 历史保留 |
| --- | --- | --- | --- | --- |
| `new` | 参与 | **按团队平均计**（均衡加分为 0，不被驱动填坑） | **一票否决**（3分任务不参与） | 保留 |
| `active` | 参与 | 正常（`(团队平均 − 本人当前周积分) × 5`） | 受 `maxHeavyTaskCount` 约束 | 保留 |
| `rest` | **永不参与**（硬性过滤「休假中」） | 从团队平均中排除 | — | 保留 |
| `left` | 永不参与 | 从团队平均中排除 | — | 保留（历史/统计/导出仍含） |

- **转正**：领导在配置页手动将 `new` → `active`，解除新入保护。无自动转正。
- **休假**：配置页卡片开关直接切换（关=休假，开=恢复 `restFrom` 记录的休假前状态，`new` 休假恢复仍是 `new` 不自动转正）；休假是长期状态，区别于按天记的请假记录（Leave）。
- **退出**：仅在配置页编辑弹窗的状态下拉中处理，状态机单向最终，不可逆（恢复需人工重新添加或编辑改回）。

### 3.4 Schedule（排班记录）

| 字段 | 类型 | 说明 | 示例 |
| --- | --- | --- | --- |
| `id` | String | 排班记录 ID | `"SCH_001"` |
| `date` | String | 日期 `YYYY-MM-DD` | `"2026-08-30"` |
| `projectId` | String | 对应任务 ID | `"P101"` |
| `slotLabel` | String | 时段标签（引用 `Project.slots[].label`） | `"早"` |
| `staffIds` | Array | 已分配人员 ID 列表 | `["S001","S002"]` |

> **无 `status` 字段**：排班改即生效，无草案/确认两态。

### 3.5 Leave（请假记录）

| 字段 | 类型 | 说明 | 示例 |
| --- | --- | --- | --- |
| `id` | String | 唯一标识 | `"LV_001"` |
| `staffId` | String | 请假人 ID | `"S001"` |
| `date` | String | 请假日期 `YYYY-MM-DD` | `"2026-08-30"` |
| `reason` | String | 原因（可选） | `"家中有事"` |

- 请假按"人 + 某天"记录，**连续多天 = 多条 `date` 记录**（不引入 dateRange，保持轻量）。
- 请假当天该人员视为不可参与（硬性过滤排除）。

---

## 4. 核心算法

### 4.1 展开（expand）

```
展开 = 仅「批量铺排」按钮触发（expandWeeks，幂等去重），或手动建班次。
周历渲染不再自动展开（ensureExpanded 已移除）——删除班次不会被自动复活；
新任务需批量铺排或手动建班次后才出现在周历。
选中某周 → 遍历所有 active 项目 × 其 weekDays 命中该周的天
        → 按项目每个 slot 生成班次（date + projectId + slotLabel + staffIds=[]）
一次性任务（weekDays=[]）不自动展开，领导手动建班次。
```

### 4.2 硬性过滤（filter，一票否决）

对候选人员，符合任一条件**严禁进入候选池**，并**返回被拒原因**：

1. `bannedProjects` 包含当前项目（含录入原因）。
2. 不具备 `allowedProjects` 权限。
3. **当日任务数超限**：该人员当天已排班次数 + 1 将超过 `dailyTaskLimit`（默认 2）。
4. **时段任务数超限**：该人员同一时段（自主安排/早/中/晚）已排班次数 + 1 将超过 `slotTaskLimit`（默认 1）。
5. 加入后本周累计疲劳将超过 `maxWeeklyFatigue`。
6. 高强度项目（3分）且加入后本周高强度次数将超过 `maxHeavyTaskCount`。
7. **休假中**：状态为 `rest`。
8. **请假中**：当日有 `Leave` 记录。
9. **已退出**：状态 `left`。
10. **新入保护**：状态 `new` 且该项目 `fatigueScore === 3`。

### 4.3 打分（score）

对通过硬性过滤的候选：

$$Score = 擅长加分 + 均衡加分$$

- **擅长加分**：该项目在 `preferredProjects` 中 → **+15**（附录入原因）。
- **均衡加分**：`(团队平均周积分 − 该人员当前周积分) × 5`
  - **团队平均** = 全部 `active` 状态人员的周积分平均值（**排除 `left` 与 `rest` 状态人员**；`new` 人员按团队平均取值参与计算，即其均衡加分为 0；请假不改变平均基数）。
  - 周积分 = 本周（周一~周日，**固定自然周**）该人员所有已排班次的 `fatigueScore` 之和。
（间隔保护扣分已随「具体时间」一并移除，负荷由 4.2 数量上限 + 周疲劳控制。）

### 4.4 智能排班（自动填充）

```
领导点击"智能排班"按钮（手动触发，不自动）：
1. 取当前周视图内所有空班次（staffIds=[]）。
2. 排序：先按日期，同一天固定时段（早/中/晚）优先、自主安排最后（自主安排不占时段，留到软约束捡漏）。
3. 逐个班次：硬性过滤 → 打分 → 分配得分最高者 → 立即更新该人周积分 → 下一班次。
   （逐个更新周积分，保证后续班次的均衡加分感知前面分配。）
```

### 4.5 替补推荐（substitute）

```
领导对某人某天标记请假（按"人+某天"整处理）：
1. 收集该人员当天全部班次。
2. 对每个班次：剔除该人员 → 硬性过滤（跳过请假人） → 打分 → 取 Top3。
3. 输出：弹窗逐班次展示 Top3 + 推荐理由 + 一键替换。
```

### 4.6 可解释性（Decision Reasoning）

算法返回**结构化原因**，而非仅结果：

- 过滤返回：`{ rejected: true, reasons: ["黑名单：腰伤，不宜搬重物", "本周疲劳将超限"] }`
- 打分返回：`{ staffId, score, breakdown: [{label:"擅长搬运+15", reason:"体力好"}, ...] }`
- 替补弹窗、智能排班、拖拽阻止，均展示原因明细。

---

## 5. 功能模块与 UI 交互

```
[基础配置页] ➔ [周历网格看板] ➔ [智能排班/拖拽调整] ➔ [请假替补弹窗] ➔ [疲劳分析/导出]
```

### 5.1 基础配置页（config）
- 人员/任务均以**卡片网格**呈现（响应式多列，非表格）：头部=名称+状态徽标，字段行标签化，底部操作区=开关+编辑。
- 人员卡片：**参与/休假开关**（关=休假 `rest` 并记录 `restFrom`，开=恢复原状态；`new` 休假恢复仍是 `new`），退出/转正在编辑弹窗的状态下拉处理；排序=已退出殿后、其余按 `joinedAt` 倒序（新加入在前）。
- 任务卡片：**启用/停用开关**即时切换 `active`。
- 编辑弹窗（卡片「编辑」按钮）：编辑全部字段，含 `preferredProjects`/`bannedProjects` 的**原因录入**、`status` 状态管理（新入/活跃/休假/退出）。工具栏含「设置」弹窗（改数量上限与预警阈值）。
- Excel 模板导入 / 导出（人员、任务）。
- 一键导出全部数据为 JSON 备份。

### 5.2 周历网格看板（calendar）
- **行 = 预置时段标签（自主安排/早/中/晚），列 = 周一~周日**，行固定、不随项目动态增减，保证多项目对齐；**自主安排行置顶且高度较小（0.55fr）**，早/中/晚均分剩余；卡片不显示时间。
- 单元格 = 该时段展开的班次卡片（任务名 + 已分配人名）。
- **手动建班次**：点击空白单元格 → 选择任务（默认列出一次性任务）→ 创建班次，用于临时/一次性任务；**已有班次的单元格底部有「+ 再建一个」按钮**（虚线圆钮，hover 高亮），可在同一时段追加多个班次。
- **批量铺排**：工具栏按钮 → 弹窗输入 N（1-4）周 → 对接下来 N 周按 active 项目 weekDays+slots 展开班次空壳（幂等去重、不分配人员），用于提前计划。
- 周切换器查看任意周。
- **拖拽人名**到其他班次：实时硬性校验，非法移动显示原因 + 红框警告并阻止。
- 空班次显示"+ 智能排班"入口（触发 4.4）。

### 5.3 请假与替补（modal）
- 点击人名 → "标记请假" → 弹窗列出该人当天全部班次 → 逐班次 Top3 + 推荐理由 + 一键替换。
- 替换后原班次人员更新，请假人当日记录保留在 `Leave`。

### 5.4 疲劳分析（analysis）
- 本周全员柱状图：累计劳累积分 / 高强度次数双指标，横向对比。
- 超限人员红色标注；点击人员可下钻查看其得分构成/被拒原因。

### 5.5 导入导出
- Excel：仅人员/任务基础配置导入（班次不导入）；**排班结果可导出为 `.xlsx` 考勤表**（按周/月，含日期、任务、时段、人员）。
- JSON：全量备份 / 恢复。

---

## 6. 关键参数（可配置）

| 参数 | 默认 | 说明 |
| --- | --- | --- |
| `dailyTaskLimit` | `2` | 一人一天任务数上限（达上限 +1 硬性拒绝） |
| `slotTaskLimit` | `1` | 一人一时段任务数上限（达上限 +1 硬性拒绝） |
| `warnDailyCount` | `2` | 预警阈值：当天任务数达该值 chip 变黄 + 排班 toast 提醒 |
| `preferredBonus` | `15` | 擅长加分 |
| `balanceFactor` | `5` | 均衡加分系数 |
| `heavyFatigueThreshold` | `3` | 高强度判定阈值 |

---

## 7. 变更记录

- 2026-08-31：**周历导出图片**（`src/ui/exportImage.js`，Tao 迭代拍板；**外部依赖新增 html2canvas**——突破「仅 SheetJS」约束，esbuild 内联进单文件、file:// 双击可用不受影响）。①**入口**：周历工具栏「导出图片」按钮（总览/项目/人员三维度均有），点击异步生成 + toast 反馈；**空态防御**：过滤视图无班次时 `.cal-grid` 未挂载，点击 toast「当前视图暂无班次」不导出。②**截图方案**：离屏构造「浅紫画布 + 白色圆角内容卡 + 标题 + 周历克隆」整体交给 html2canvas（`scale:2` 高清，DPR 经验见 pitfalls#11），原页面零扰动；**不可把标题 prepend 进 `.cal-grid` 内部**——它是 CSS Grid 容器，标题会被当作网格项塞进周一列（挤左上角+内容下推）；**html2canvas `onclone` 修改克隆元素不改变 canvas 尺寸**（canvas 由原 DOM 测量），需变大画面时必须用离屏包装容器整体截图。③**固定宽度**：周历 1200px（7 列均分 ~161px），导出大小与浏览器窗口/缩放无关；四周 30px 留白 + 白卡内边距 22px，不切边。④**标题区**（卡内顶部，居中）：第一行周范围 `2026-08-31 ~ 2026-09-06`（20px 加粗品牌紫）+ 分隔线；第二行左=视图标注（`总览`/`任务：X`/`人员：X`，随维度联动）、右=导出时间（`导出时间：YYYY-MM-DD HH:mm`）。⑤**静态视图**（Tao 指出：导出的是视图不是功能）：克隆时移除全部功能性 UI——`.cal-today`（今天表头高亮）、`.cal-today-flag`（小旗子）、`.cal-add-day`（表头建班次入口）、`.sch-smart`（空班次智能排班按钮），仅保留信息内容（任务名/时段/人员/容量）。⑥文件名 `Numbers-排班图-YYYY-MM-DD.png`（周一起始日）。

- 2026-08-31：**周历视图维度切换（总览/项目/人员）+ 拖拽落点精确到卡片**（纯渲染层，数据模型/算法零改动）。①**维度切换器**：工具栏左侧「总览 | 项目 | 人员」分段按钮（`.seg.seg-sm`），选项目/人员后联动出现**可搜索下拉**——`createSelect` 新增 `searchable` 选项：面板顶部搜索框、输入即过滤、无匹配提示、键盘 ↑↓/回车照常、面板内点击不冒泡到 document（防误关闭）。②**项目维度**：只显示该任务班次（保留时段大卡片四色结构与全部编辑能力：表头建班次预选当前任务、智能排班/批量铺排**只作用于当前任务**（toast 带「已为「X」填充」前缀）、分配/删除/拖拽照旧）；摘要行显示「本周 N 个班次 · 已排 X/Y 人 · 缺 Z/已满员」。③**人员维度（仅视图，Tao 拍板）**：隐藏智能排班/批量铺排/建班次入口，卡片不可开分配弹窗（`cal-grid.readonly`）、人名不可拖拽；保留人名点击→标记请假替补；无班天显示「未排班」虚线占位（`.cal-day-off`）；摘要行显示「本周 N 个班次 · 疲劳 x/上限 · 高强度 y/上限」+ 状态标签（新入/休假中/已退出）。④**摘要按当前浏览周现算**：`ctx.weeklyFatigue` 锚定首个班次所在周（`buildContext` 用 `schedules[0]?.date`），切周浏览会失真，摘要不依赖 ctx。⑤**维度状态持久化** `is_sched:cal_view`（`{mode,id}`），作为渲染参数持有、不依赖 DOM 反读（操作后重渲染不跳维度）；目标被删除自动回退总览；切维度沿用仍存在的目标、否则自动选第一个。⑥**拖拽落点修复（bug）**：`enableDrop` 从时段格下沉到任务卡片级，`dropStaff` 按班次 ID 定位目标——同格多班次不再「取第一个」（旧实现会静默落到数组序第一的班次甚至原地空转）；新增防御：原地拖放无操作、拖到已含此人班次拦截提示（旧实现 Set 去重名单但 ctx 加分未防，会**重复累加**疲劳/次数计数）。整周无班次显示空态卡片（`.cal-empty`）。

- 2026-08-29：**排班页周历布局改造（设计定稿，待实现）**。①**每天一列、去整列底色与时段行**：周历 7 天列，列间留隙，不再有「自主安排/早/中/晚」四行网格，也不再有整天列底色带。②**列内时段「大卡片」**：每天列内最多 4 个时段大卡片（自主/早/中/晚）竖向叠放，**只渲染当天有班次的时段**、空时段不显示；大卡片 = 时段固定底色（**全周一致**：自主=浅紫灰 `#f1e8f6`、早=浅玫瑰粉 `#f9e8ef`、中=浅薰衣草 `#ece4f8`、晚=深紫 `#e6dcf0`）+ 右上角时段 chip + 内部任务卡片（白底 + 时段色边框）自然高度堆叠。③**建班次入口上移表头**：每列表头（周几+日期）提供「＋ 添加」按钮（**hover 才显现**）→ 弹窗选时段+选任务；移除空单元格/卡片内手动建班次入口。④**滚动策略**：卡片高度随内容自适应，正常情况完整展开不滚；任务爆多极端天允许溢出滚动兜底。⑤空班次（`staffIds=[]`）卡片保留「＋ 智能排班」；今天表头保留紫色底+小旗子（表头既有实现）。⑥**数据模型/算法零改动**，纯渲染层：calendar.js 渲染结构重构 + theme.js 新增时段大卡片/表头入口样式；filter/score/expand/store 不动。
- 2026-08-28：**JSON 数据备份入口改造**。①JSON 全量备份/恢复从基础配置页工具栏移除，移到**侧边栏导航「数据备份」**（紧跟疲劳分析，作为第 4 个导航项，由 buildNav 生成）。②点击弹「数据备份」卡片式弹窗：顶部安全提示条 + 导出备份/恢复数据两张操作卡片（品牌渐变图标方块、hover 上浮、stagger 入场）+ 底部覆盖警示。③`openModal` 新增可选 `boxClass` 参数（备份弹窗 760px 加宽，其他弹窗不受影响）。④导出文件名 `Numbers-YYYYMMDD-HHMMSS.json`。⑤JSON 全量备份（`exportJSON`/`importJSON` 含人员/任务/班次/请假）是**唯一全量兜底**，保留不删；存储演进（文件系统备份、按周分桶）Tao 决定搁置。
- 2026-08-28：**Excel 导入导出规范**。①表头中文化、按编辑弹窗字段顺序、备注取值/默认值（如 `周疲劳上限(默认6)`）；**模板（下载填写）无 ID 列**，导出（存档/迁移）保留 ID 保证引用关系，导入兼容两种。②多值字段统一**分号分隔**，全角/半角（逗号、分号、括号、冒号）全兼容。③擅长/不合适项目用 **`项目(原因)`** 格式（如 `P101(体力好,搬运熟练);P102`），不再用 JSON——原因内可含逗号，条目间分号分隔（解析仅按分号切，原因内逗号保留）。④状态列导出/模板均为**中文**（新入/活跃/休假/已退出），导入兼容中英文。⑤任务补上此前遗漏的 `timeRange`（时间段开始/结束两列，非法或倒挂导入为 null）。⑥导入过滤：人员引用的项目不存在时忽略该字段值（allowedProjects/擅长/不合适均过滤）。⑦新增**导入弹窗**（点击 Excel 导入）：下载模板 + 选择文件导入；模板含 `【示例】` 示例行（导入自动跳过）。⑧时段/重复星期用分号分隔标签。⑨导出文件名统一 `Numbers-` 前缀：人员表/任务表/**排班表**（原考勤表更名）/模板。
- 2026-08-29：**模板说明标注 + 导入去重覆盖**。①任务/人员模板列统一标注**必填/选填 + 中文含义**：`劳累指数(必填;1=轻松,2=中等,3=高强度)`、`时段(必填;自主安排/早/中/晚,分号隔开)`、`启用(选填;1=启用,0=禁用,默认1)`、`状态(选填;新入/活跃/休假/已退出,默认活跃)`、`重复星期(选填;1-7;分号隔开;1=周一…7=周日;空=一次性任务)` 等；选填列带默认值。②**重复星期外部格式 1-7**（1=周一…7=周日），内部存储仍 0-6（0=周日）；模板/导出用 1-7，导入 `7→0` 映射并兼容旧 0-6 文件。③**时间段导入修复**：Excel 原生时间存为序列号，`sheet_to_json` 默认 raw 读回浮点数导致时间段静默丢失；改 **`raw:false`** 返回格式化文本（如 `08:00`）+ 解析层 `toHHmm` 规整化（兼容无前导零 `8:00`、全角冒号；数字序列号防御分支）。④**导入按名称去重覆盖**：同名或同 ID 覆盖（任务保留原 ID、人员保留原 ID+`joinedAt`），不同名新增，空名跳过，toast 统计「导入 N 个：新增 X、更新 Y」；文件内多行同名后者覆盖前者。⑤**手动新增/编辑重名校验**：任务/人员名称 trim 后与其他记录（排除自己）重复 → 行内报错禁止保存；保存时名称统一 trim。⑥表头改版后导入用 **KEY_ALIAS 归一化**旧表头键名，旧导出文件可重导入（最原始表头：`劳累指数(1-3)`/`重复星期(0-6)` 等）。
- 2026-08-28：周历班次卡片手动分配人员。①点击班次卡片主体（任务名/meta/空白区）弹「排班分配」弹窗；人员 chip 点击仍为请假替补（stopPropagation 隔离）。②弹窗布局：大标题（任务名+劳累徽标）+ 副行（日期·时段·时间范围）+ **容量进度条**（紫色填充，满员变绿「✓ 已满员 X/Y」）+ 分区标题（已排人员/可选人员）+ 已排人员胶囊 chip（带 × 移除，hover 浅红；移除后周疲劳/高强度/日任务数/时段数计数回退）+ **全量候选列表**（可加入者 hover 浅紫底+滑入「＋ 添加」按钮，点击即排并 toast 反馈；被硬性过滤者灰化+红字原因；满员显示「✓ 本班次已满员」）。③加入/移除均走 saveSchedule + ctx 计数同步更新（均衡打分即时感知），达预警阈值 toast 提醒。④关闭按钮复用 openModal 自带（不再自定义）。⑤**拖拽修复（两处）**：a) dropStaff 拖拽移人从 A 班次到 B 班次是「一减一增」（源移除+目标加入，均持久化），原实现只改显示层、**ctx 计数（周疲劳/高强度/日任务数/时段数）不更新**导致拖走后算法层仍认为人占源班次名额（表现如叠加）；现移除/加入均同步 ctx 并 toast 反馈「已调整至 X 时段」。b) **移动语义校验**：拖拽是移动而非新增，原实现用「当前计数+1」校验导致同一任务跨时段移动被误报「本周高强度次数将超限」；现构造「移走源班次后」的计数快照（ctxAfterMove）再校验。c) 新增**目标容量校验**：目标班次已满时阻止拖入（toast「目标班次已满员」）。d) **拖拽高亮防抖**（dnd.js）：鼠标在格子边框边缘时 dragover/dragleave 快速交替导致高亮闪烁，dragleave 改延迟 100ms 移除（边缘快速回归时取消移除）。
- 2026-08-28：**班次删除 + 渲染不再自动展开**。①「排班分配」弹窗 footer 左侧新增「删除班次」红色按钮（两步确认：首次点击变「确认删除？」3s 恢复，二次点击执行；删除后已排人员周疲劳/高强度/日任务数/时段数计数回退，toast「班次已删除」）。②**移除渲染时自动展开（ensureExpanded）**：原实现每次渲染周历按 active 项目 weekDays+slots 补齐缺失班次，导致**删除的班次刷新后自动复活**；现展开仅由「批量铺排」触发（见 4.1）。③**已有班次的单元格底部加「+ 再建一个」按钮**（`.cal-add` 虚线圆钮 hover 高亮），解决「有班次后无法再手动建班次」。
- 2026-08-28：任务选填「时间段范围」（仅展示）。①Project 新增 `timeRange {start,end}`（HH:mm，选填），校验：填了则格式合法且结束晚于开始（`isValidTimeRange`）。②新增/编辑任务弹窗加选填行：**自建时间选择器 `src/ui/timepicker.js`**——点击整个框弹出时/分双列面板（小时 00-23、分钟 00-59 全量，选中项浅紫底色，选分钟自动收起）；**倒挂即时校验**：开始 ≥ 结束时行内即时标红提示「结束时间需晚于开始时间」，保存时 model 校验再拦截（双保险）；只填一端报错「开始与结束时间需同时填写」。③展示：配置页任务卡片字段行（时钟图标+时间）、周历班次卡片 meta 小字（`.sch-time`）；**不参与 filter/score/expand，不做冲突检测**（数量上限方案不变）。④新增 ICON_CLOCK；时段行删除按钮文本 × 改 15px SVG 图标（hover 浅红底+红字，`.row-del`）。
- 2026-08-27：配置页卡片化 + 人员休假状态。①人员/任务列表改**卡片网格**（`.card-grid` 响应式多列，头部姓名+状态徽标、字段行、底部操作区）。②**人员新增 `rest` 休假状态**：卡片开关「参与/休假」（关=rest 记录 `restFrom`，开=恢复原状态，new 休假恢复仍为 new）；退出仅在编辑弹窗处理；`filter` 硬性排除「休假中」；`computeTeamAvg` 排除 rest。③新增 `joinedAt` 加入时间（createStaff 自动记录），卡片排序=已退出殿后、其余按加入时间倒序；编辑保存保留 joinedAt 不重置。④任务卡片「启用/停用」开关即时切换。⑤编辑按钮改幽灵样式（.btn-ghost）。⑥新增 .switch 开关组件（紫色轨道滑块）。
- 2026-08-26：时段体系重构（**去时间 + 数量上限预警**，实现完成；由上午/中午/下午/晚上演进而来）。①时段标签 = 自主安排/早/中/晚，**全部无具体时间**（移除 DEFAULT_TIMES/fillSlotTimes/timeRange 组件），任务至少一个时段、时段仅标签。②**数量上限替代时间冲突**：filter 新增「当日任务数 ≤ dailyTaskLimit(2) / 一时段任务数 ≤ slotTaskLimit(1)」硬性过滤（自主安排计入数量），移除 hasTimeOverlap 与间隔保护扣分。③**预警**：当天任务数达 warnDailyCount(2) → 人员 chip 变黄 + 智能排班 toast 提示。④settings 存 is_sched:settings，配置页「设置」弹窗可改三项。⑤周历 4 行：自主安排置顶且高度 0.55fr（较矮），早/中/晚均分剩余；卡片不显示时间。⑥智能排班先固定时段（早/中/晚）后自主安排。⑦旧数据清空重配（历史时段标签不兼容）。
- 2026-08-26：侧边栏修复。①收缩/展开左上角品牌区高度恒定（收缩态 `.side-brand` padding 对齐展开态 13px，消除 6px 跳动）。②消除刷新 FOUC 裸闪：首屏侧边栏整套样式内联进 index.html `<style>`，theme.js 仍注入全量作覆盖；**两处样式需同步维护**（改侧边栏样式时 index.html 首屏段与 theme.js 侧边栏段一致）。
- 2026-08-26：存储层写放大优化。①db.js 内存 Map（id→record）索引：put/remove O(1)、getAll 免 JSON.parse，新增 `writeAll` 一次整表替换。②store.js 增量缓存：saveXxx/removeXxx 只更新 cache 不再 loadAll；loadAll 仅初始化/导入/重置；importJSON 用 writeAll 批量（O(n²)→O(n)）。③数据流铁律：**cache 唯一真源**，视图统一 getCache() 读，main.js 入口 loadAll 一次。④测试：新增 store.test.js 9 例 + db writeAll 用例。**局限**：写放大（整表 stringify）仍在，需 B 档按周分桶消除；db 内存 Map 后多标签页不同步（单标签场景无碍）。
- 2026-08-26：品牌与视觉升级。①**品牌**：浏览器标签/左上角改「民数记 Numbers」，品牌区双层（主标题+紫色小字副标题）、蒜图标渐变圆角容器。②**色系**：蒜皮紫 → **莓果梅紫**（主 #5a1d78 / hover #48115f / active #380c4a / 浅底 #f7f1fa / 活跃底 #efe3f6 / 边框 #e0d2ef / 深边框 #c9b0e0 / 页面底 #f7f4f8），theme.js 全量替换、无旧值残留。③**UI 精致化**：阴影改双阴影并带紫偏置、卡片 hover 上浮、中性色（文字/边框/背景）全向紫偏置、聚焦外发光柔和至 .14、弹窗/下拉/toast 双阴影、遮罩改深紫黑。④**疲劳分析页**：canvas 缓冲 × devicePixelRatio + setTransform（解决高分屏模糊）、图表容器 flex:1 弹性填满（消除下方空白）、空态提示、网格线带紫。
- 2026-08-25：周历排班页大优化 + 全局紫色设计语言。①**时区铁律**：日期一律本地 `todayStr()`/`toDateStr()`，禁用 `toISOString().slice(0,10)`（UTC 偏移致周切换错乱/凌晨定位错周，全项目 5 处修复）。②**布局**：body `height:100vh + overflow:hidden`，内容在各滚动容器，周历 grid `minmax(0,1fr)` 均分填满视口消除空白。③周历信息增强：班次卡片显时段时间+劳累指数徽标+缺员/满员角标，人员 chip 疲劳状态色（超限红/接近橙/正常），今天列高亮，**批量铺排未来 N 周**（`core/expand.js` 新增 `expandWeeks`）。④select 下拉面板 `fixed` 视口定位防弹窗内裁剪。⑤色系蓝→**蒜皮紫**（主色 #7c3aed、浅紫 #f5f3ff/#ede9fe、紫边框 #ddd6fe/#c4b5fd）。⑥config/analysis 两页视觉统一（分段Tab/状态徽标/摘要卡/圆角渐变柱）。
- 2026-08-25：UI 四轮打磨。①侧边栏：顶部 tab 改左侧可收缩侧栏（浅色、内联 SVG 线框图标、收起成 56px 图标窄条、localStorage 持久化）。②新增 `src/ui/select.js` 自定义下拉组件：单选/多选统一（选项打勾/键盘导航/多选标签增删/点击外部关闭），替换全部原生 select。③按钮交互反馈：按压 scale、hover 投影、active 内阴影、圆角 8px。④表单元素统一：周几选择胶囊 `.day-chip`、输入框 hover/圆角、周历卡片 hover 上浮+虚线空单元格、表格斑马纹。设计语言定调：8px 圆角、主色 `#2563eb`、按压/悬停反馈。
- 2026-08-24：基于探讨版 PRD 与 Tao 逐项对齐，定稿本 spec。新增：时段级排班、周历网格、人员状态机（new/active/left）、原因解释机制（可解释排班）、请假记录表。
- 2026-08-24：存储层 IndexedDB → localStorage。原因：file:// 双击打开时 Chromium 的 IndexedDB.open 永不回调导致空白页；localStorage 在 file:// 下可读写且持久，实现「双击即用」。
- 2026-08-24：UI 打磨定稿（按钮+表单+组件一致化）。新增 `src/ui/theme.js`（设计令牌+全局样式注入）与 `src/ui/fields.js`（结构化表单），编辑弹窗告别裸 JSON 输入、校验错误行内化；`validateProject/validateStaff` 返回结构化 `{field,msg}`。
