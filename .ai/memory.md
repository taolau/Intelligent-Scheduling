# [时间] 2026-08-26
# [定位] 实时上下文：当前 Sprint 进度缓存
# [作用] 记录"现在"正在做的事。任务完成后需及时清理。
# [规则] 重点标注 Tao 的进度。

## 📍 当前状态
- **侧边栏修复**：✅ 完成（待提交）- ①收缩时左上角品牌区高度跳动：theme.js 收缩态 padding 16→13 对齐展开态，三态恒 65px；②刷新 FOUC 裸闪：首屏侧边栏样式内联进 index.html，theme.js 注入覆盖作双保险
- **存储层 A 档优化**：✅ 完成（已提交 855c314 并 push）
- **存储演进研讨**：⏳ 进行中 - 桌面文件存储：纯前端 file:// 下「全自动+固定路径+免授权」不可能三角；方案 A（每次点一次目录）/ B（OPFS 无感）/ C（本地应用）待 Tao 定夺
- **B 档优化**：⏳ 待研讨 - 按周分桶消除 schedules 单 key 无界增长（撞 5MB）；版本号 + 迁移机制
- **dist**：⏳ 旧构建，需 `node build.js` 更新
- **品牌/色系/UI/分析页**：✅ 完成（已提交）
- **时段体系重构（去时间）**：✅ 实现完成（待提交）- 时段=自主安排/早/中/晚 纯标签无时间；数量上限+预警替代时间冲突（日2/时段1/预警2，设置可改）；周历 4 行（自主安排矮 0.55fr，早中晚均分）；卡片无时间；chip 当日达预警阈值变黄+智能排班 toast；旧数据清空重配；单测 59 全绿+手测通过

## 🧠 核心决策
- **存储数据流铁律**（长期，已入 spec）：cache 唯一真源，视图经 getCache() 读；saveXxx 增量更新缓存不重读；loadAll 仅初始化/导入/重置
- **db 内存 Map 索引**（长期，已入 spec）：O(1) CRUD、首次访问才从 localStorage 加载；writeAll 一次整表替换；局限=多标签页不同步（单标签场景无碍）
- **file:// 存储边界**（长期约束）：showDirectoryPicker 可用但目录句柄无法跨会话持久化，每次重开需重新授权；「自动+固定路径+免授权」不可能三角
- **首屏样式双源**（本次）：侧边栏首屏样式内联 index.html 消除 FOUC，theme.js 仍注入全量覆盖；**改侧边栏样式需两处同步**（index.html 首屏段 + theme.js 侧边栏段，注释锚点已标记）
- **数量上限+预警替代时间冲突**（本次，去时间）：时段标签=自主安排/早/中/晚 均无时间；硬性过滤按「一人一天最多 N 个 / 一人一时段最多 M 个」计（自主安排计入数量）；达预警阈值（默认=日上限）chip 黄+toast；settings 存 is_sched:settings，配置页「设置」弹窗可改；间隔保护已移除

## ⚠️ 待办与注意
- [待办] 提交侧边栏修复（theme.js + index.html）+ 记忆文件改动（[知识库] 一笔）
- [待办] 存储演进定案：A 档下一步（桌面文件方案 A/B/C）+ B 档（按周分桶/版本迁移）研讨
- [待办] dist/index.html 旧构建，需 `node build.js` 更新
- [待办] 提交：侧边栏修复 + 时段体系重构（去时间+数量上限预警）+ spec/memory 更新（视改动量合 1-2 笔）
- [注意] 时区陷阱已入 pitfalls：本地凌晨/周切换会踩
- [注意] 拖拽回归手测待做：Playwright 无法模拟 dataTransfer（pitfalls#4）
- [注意] 样式 JS 注入有 FOUC：首屏静态部分需内联 index.html（pitfalls#14）
