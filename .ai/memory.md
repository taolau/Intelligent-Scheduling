# [时间] 2026-09-07
# [定位] 实时上下文：当前 Sprint 进度缓存
# [作用] 记录"现在"正在做的事。任务完成后需及时清理。
# [规则] 重点标注 Tao 的进度。

## 📍 当前状态
- **09-05 已提交**（2030025 批量铺排）——已清账
- **09-07 批次 1（未提交）——智能排班「预览 + 确认」改造**：core/auto.js simulateAutoFill 同通道预览、缺口单列、批量落盘；105 单测全绿；待 Tao 自查后与批次 2 一起提交
- **09-07 批次 2（未提交）——人员标签 + 粒度导航微调 + UI 微调**（含 Tao 验收反馈三轮修订，均实测）：
  - **人员标签**：Staff.tags 自由文本多标签（任意字符、同人同标签去重、全库共享）；编辑弹窗标签 = **同框可输可选 chip 控件** tagsInput（聚焦浮出已有候选、键入过滤、点选/↑↓+回车添加、无匹配回车自创、× 移除）；**卡片行序 = 可胜任→擅长→不合适→标签**（spec 5.1 已同步）、**弹窗内标签字段置最底**；人员 tab 头部标签多选过滤（与名称搜索叠加与关系，无人有标签不显示）；排班视图人员维度标签多选过滤（页内临时态，目标自动回退）；分配/替换弹窗候选标签过滤（视图层不动算法）；Excel 人员模板「标签」列（; 分隔，parseTags/formatTags 收 model.js 纯函数）；spec 3.2/5.1/5.2/5.5、project_map、pitfalls#35 已同步
  - **粒度导航微调（Tao 拍板）**：周粒度按钮「今天」→「本周」；周↔月切换回默认锚——spec 5.2 已改
  - **编辑弹窗布局（Tao 反馈定稿）**：姓名+状态同行（.field-pair 双列类，theme.js，顺带收编上限行原内联 flex）；字段序 = 姓名状态 / 上限行 / 可胜任 / 不合适 / 擅长 / 标签
  - **标签候选浮层规格（Tao 反馈三轮定稿，fields.js positionPanel）**：宽度**恒等于控件全宽**（左缘=容器左缘，chips 增减不移位，旧版锚输入框宽度随 chips 跳变被否）；垂直锚**控件外框底缘 +4px**（旧版锚输入框底缘实贴边框≈0 间距被否）；selIn 弹入动画、选项行同 .sel-opt 规格；**回车键序 = 有候选高亮先选中（空输入也可键盘/悬停选择）→ 无匹配输入才自创 → 否则收起**（旧版空输入回车只收起、键盘流断裂）；hover 行同步键盘高亮（所见即所选）
  - 实测：Playwright 走通 chip 输入/候选排除/过滤/空态/浮层恒定宽度/hover+回车选中/姓名状态同行/标签位置；**修了 Enter 键盘断点真 bug**；测后 dev 数据已还原（两人标签清空、状态活跃）
  - **弹窗候选过滤（Tao 反馈定稿）**：分配/替换弹窗过滤栏 = 「搜姓名」输入（常显）+ 窄标签多选（126px），紧凑左置，名称/标签与关系叠加收窄候选展示（算法名单不变）；标签下拉实际撑全宽是 **.sel { width:100% } 同特异性后置覆盖致 170px 声明从未生效** 的静默 bug——修法 `.sel.asg-tag-sel` 双类提升（pitfalls #36，新增）；分配弹窗切行 display 不重建 DOM（输入不失焦、空态分因文案）、替换弹窗组内 Top3 卡重滤；spec 5.2 已同步；实测两弹窗名称/标签/组合/空态/清空全过，dev 数据已还原
  - **分配弹窗候选两区化（Tao 拍板）**：候选列表拆「可添加 N」/「暂不可添加 N」两区（受限灰行 + 原因独立副行），区头计数随过滤联动、区空自隐、两区皆空才显空因文案；spec 5.2 已同步；实测分组/过滤联动/受限原因副行/满员路径全过，dev 数据已还原
- **dist**：⏳ 旧构建（09-03），需 `node build.js`（Tao 要求时才构建）

## 🧠 核心决策
- 09-07 标签功能：**标签 = 自由文本分组元数据**（不参与算法，只做人选浏览的视图层收窄）；一人多标签但同标签不重复；输入形态 Tao 拍板 = **同一输入框可输可选**（聚焦即出已有标签候选，无匹配回车自创）——下拉与输入框分开两个控件被否（「脱节」）
- 09-07 弹窗候选过滤定稿：**名称搜索（常显）+ 窄标签多选并排左置**、双条件与关系叠加（Tao 拍板——过滤栏控件应紧凑一体，不做宽下拉孤悬一侧）
- 09-07 UI 验收定稿：人员卡片标签行 = 可胜任/擅长/不合适之下（spec 5.1 已同步）；编辑弹窗姓名+状态同行、标签字段置最底；浮层观感与已有下拉 .sel-panel 完全同构（宽度恒定、外框 +4px、同动画同行高）
- 09-07 智能排班三件套（已落 spec 4.4/5.2）：**范围 = 当前浏览范围**、**预览 = 模拟执行**（simulateAutoFill 同通道）、执行按预览结果一次批量落盘；缺口班次逐条单列
- 09-07 粒度导航：**周↔月切换回默认锚**（可预期，不沿用另一粒度浏览位置）

## ⚠️ 待办与注意
- **待办**：Tao 自查本轮 UI 微调（浮层宽度/间距/回车语义观感）→ 自查通过后批次 1 + 批次 2（含 UI 微调）一次 /commit
- **注意**：Playwright 还原测试数据时勿用 evaluate 直点背景卡片/按钮——会**绕过 modal 遮罩**开出弹窗栈，后续「保存」按钮选择器可能命中底层弹窗 → 误保存/还原失败（本次翻车一次：先删 chip 后保存落错弹窗）；还原后核对卡片行 + 弹窗数（modalCount=0）再收尾
