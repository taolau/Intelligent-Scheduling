# [时间] 2026-08-28
# [定位] 实时上下文：当前 Sprint 进度缓存
# [作用] 记录"现在"正在做的事。任务完成后需及时清理。
# [规则] 重点标注 Tao 的进度。

## 📍 当前状态
- **任务选填时间段范围**：✅ 完成（待提交）- Project `timeRange {start,end}` 仅展示不参与算法；自建 timepicker（点击弹时/分双列面板、倒挂即时校验+保存双拦截）
- **周历手动分配人员**：✅ 完成（待提交）- 点班次卡弹「排班分配」（进度条/分区/全量候选+拒绝原因/已排 chip × 移除）；**班次删除**（两步确认、计数回退）
- **拖拽三修复**：✅ 完成（待提交）- ctx 计数同步（一减一增）；移动语义校验（ctxAfterMove 防误报超限）；目标容量校验 + 高亮防抖
- **展开时机变更**：✅ 完成（待提交）- 渲染不再自动展开（删除不复活），展开仅批量铺排触发；有班次 cell 加「+ 再建一个」
- **存储演进研讨**：⏳ 进行中 - 桌面文件存储不可能三角；方案 A（每次点目录）/ B（OPFS 无感）/ C（本地应用）待 Tao 定夺
- **B 档优化**：⏳ 待研讨 - 按周分桶消除 schedules 单 key 无界增长（撞 5MB）；版本号 + 迁移机制
- **dist**：⏳ 旧构建，需 `node build.js` 更新

## 🧠 核心决策
- 本次决策已全部固化：时间选择器/手动分配/删除班次/展开时机/拖拽语义 → spec.md 2026-08-28 变更记录（多条）。本节暂空，待存储演进定案后回填。

## ⚠️ 待办与注意
- [待办] 本次 6 项改动待提交（timeRange、手动分配、拖拽修复×3、删除班次+展开时机），建议合 2-3 笔
- [待办] 存储演进定案：A 档（方案 A/B/C）+ B 档（按周分桶/版本迁移）研讨
- [待办] dist/index.html 旧构建，需 `node build.js` 更新（Tao 要求时才构建）
- [注意] 周历渲染不再自动展开：新任务需「批量铺排」或手动建班次才出现（spec 4.1）
- [注意] 拖拽回归手测待做：Playwright 无法模拟 dataTransfer（pitfalls#4，用原生 DragEvent+DataTransfer 构造）
- [注意] 改侧边栏样式需两处同步：index.html 首屏内联段 + theme.js 侧边栏段（pitfalls#14）
- [注意] 直接改 localStorage 会与 db 内存 Map 失同步：恢复数据后需刷新页面重新加载
