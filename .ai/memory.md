# [时间] 2026-09-09
# [定位] 实时上下文：当前 Sprint 进度缓存
# [作用] 记录"现在"正在做的事。任务完成后需及时清理。
# [规则] 重点标注 Tao 的进度。

## 📍 当前状态
- **09-09 Excel 导出优化批：已完成并实测，本批将提交 push**（去 ID 列 / 任务引用中文名 / xlsx-js-style 导出样式 / 列序=卡片展示顺序 / 模板只标必填；修复上限留空 Number('')=0 吞默认值 bug；spec 5.5 已同步，决策细节见「核心决策」）
- 09-09 滚动位置保持批 / 劳累指数标识批：已提交 push（spec 5.1/5.2 待同步，见待办）
- 09-08 availability 批 / 09-05 批量铺排 / 09-07 上限打磨：均已提交 push 已清账
- **dist**：⏳ 旧构建（09-03），需 `node build.js`（Tao 要求时才构建）

## 🧠 核心决策
- 09-09 Excel 导出定稿（Tao 多轮拍板；spec 5.5 已同步，本记录先行）：
  - **依赖**：`xlsx-js-style@1.2.0`（SheetJS 0.18.5 样式写入 fork）替换官方 `xlsx`——官方 CE `XLSX.write` 剥 cell.s 样式（pitfalls #18 实测属实），fork API 100% 兼容零重构、体积持平；`import * as X` 在 node 原生 ESM 取 `.default`（CJS interop，见 pitfalls #41），故统一 `X.default ?? X`。
  - **导出格式**：模板与导出均**无 ID 列**（重导入按名称匹配，旧含 ID 文件仍兼容）；**可胜任/擅长/不合适三列导出任务中文名**（`场地搬运(体力好,搬运熟练)`，任务已删或任务名含括号回退 ID 保解析）；**导入按「ID 或 中文名」双向解析**（否则导出文件重导入丢配置）；**列序 = 配置页卡片展示顺序**（任务：名称→启用→劳累→人数→星期→时段→时间段起止→加分标签→说明；人员：姓名→状态→可胜任→擅长→不合适→标签→时间安排→周/月疲劳→周/月高强）。
  - **模板表头只标「必填」（名称）**，选填不标注；留空导入自动入默认（状态→活跃、上限→系统设置默认、劳累/人数→1、启用→1），**显式填 0 保留为禁排语义**（修复了 `Number('')`=0 吞上限默认值的 bug）。
  - **导出样式**（xlsx-js-style）：表头深紫字加粗淡紫底（#5A1D78 / #E6DCF4，主题 btn-soft 同族）+ 显式四边框 + 数据行顶对齐自动换行 + 列宽内容+5 留白（钳制 14~44）+ 行高按换行撑开 + 自动筛选；**border 必须显式四边**（`all` 快捷写法不写入，见 pitfalls #42）。
  - 导出表头用简洁名（存档/给领导看），模板表头带填表说明，KEY_ALIAS 兜底三版表头均可重导入。
- 09-09 滚动位置保持定稿（Tao 拍板；spec 5.2 待同步，本记录先行）：语境键驱动恢复（排班=粒度|锚点|维度|目标、配置=当前tab）；锚定方式 = 视口顶第一可见周面板序号/配置卡 data-id + 偏移；同步渲染语义保序（inner 无 await 须同步调用，见 pitfalls #39）；tab 切换须同步语境记录。
- 09-09 劳累指数标识定稿（Tao 拍板；spec 5.1/5.2 待同步，本记录先行）：斜切三格刻度（skewX(-15°)）亮格数=劳累指数，全档统一淡杏乳 #f6cfaa + 空槽浅紫灰 #e3dbf0，svg 高 5px；位置=排班卡 title 行右端角标；三张导出图底部自动挂「任务劳累」图例。被否：鱼 SVG、档位色阶两轮。
- 09-08 字段说明文案原则（长期，spec 5.1 已固化）：hover ⓘ 通俗精简（≤ ~40 字）、禁算法术语、填 0 类特殊语义不特意说；控件 hint 与 ⓘ help 分工并存不互替。
- 测试数据红线锚定法（经验，防反复）：3 分任务每周人次 > 团队高强配额时自动铺排必超限 → 锚点周任务手动逐班指定、红线人物从自动池剔除、未来周超限噪点留给领导当调整素材（脚本内已注释）。

## ⚠️ 待办与注意
- **spec 5.1/5.2 同步**：09-09 滚动保持 + 劳累指数两批定稿并入 spec 正文章节（memory「核心决策」有全部细节）；Excel 导出批 spec 5.5 已同步。
- **dist**：⏳ 旧构建（09-03），Tao 要求时才 `node build.js`
- pitfalls #40~#43 本次新增（Vite 缓存 / CJS interop / border.all / Number('')），提交时一并入档
