# [时间] 2026-08-29
# [定位] 实时上下文：当前 Sprint 进度缓存
# [作用] 记录"现在"正在做的事。任务完成后需及时清理。
# [规则] 重点标注 Tao 的进度。

## 📍 当前状态
- **08-29 周历布局改造（本次，Tao 主导设计）**：⏳ 已实现、回归手测中、待统一提交 - 每天一列去时段行，列内时段大卡片（自主/早/中/晚 四色全周一致 + 右上角绝对定位 chip）；每列浅色基底卡片防空白；表头 hover「＋」建班次入口；空班次卡片「＋智能排班」（fillSchedule 抽取，单班次/工具栏全量共用）；数据模型/算法零改动；67 单测通过。Tao 手测确认后统一提交（含此前未提交的今天表头旗子改动）
- **存储演进研讨**：⏸️ 搁置 - Tao 决定文件备份大块先不做（方案 A/B/C、B 档按周分桶延后）
- **dist**：⏳ 旧构建，需 `node build.js` 更新

## 🧠 核心决策
- **周历布局改造定稿（08-29，Tao 逐项拍板）**：去掉时段行与整列底色，每天一列；列内按时段叠大卡片（只渲染有班次的时段）；时段四色全周一致一眼识别；建班次入口 = 表头 hover「＋」（弹窗选时段+任务），卡片内不做建班次入口；卡片自然高度、正常不滚（极端爆多可滚兜底，不承诺"永不滚动"）；每列一个浅色基底卡片防空白；时段 chip 绝对定位右上角、不占独立行
- **存储演进搁置**：文件系统备份（方案 A/B/C）、B 档按周分桶均延后，维持 localStorage 单 key 现状
- （Excel 表头规范/导入去重/备份入口等决策已固化于 spec.md 变更记录 08-28/08-29，memory 不再重复）

## ⚠️ 待办与注意
- [待办] 08-29 周历布局改造回归：Tao 浏览器手测（布局/建班次/空班次智能排班/分配/拖拽/删除/工具栏智能排班/今天标识/周切换），确认后统一提交（flag+布局+计划文档），提交后 dist 需构建
- [待办] dist/index.html 旧构建，需 `node build.js` 更新（Tao 要求时才构建）
- [待办] 存储演进（方案 A/B/C、B 档按周分桶）搁置，待 Tao 重新定夺
- [注意] 周历渲染不再自动展开：新任务需「批量铺排」或手动建班次才出现（spec 4.1）
- [注意] 改侧边栏样式需两处同步：index.html 首屏内联段 + theme.js 侧边栏段（pitfalls#14）
- [注意] 直接改 localStorage 会与 db 内存 Map 失同步：恢复数据后需刷新页面重新加载
- [注意] SheetJS 社区版 write 剥离单元格样式：模板示例行用「【示例】」前缀标识而非背景色
- [注意] 拖拽回归手测：Playwright 无法模拟 dataTransfer（pitfalls#4，用原生 DragEvent+DataTransfer 构造）
