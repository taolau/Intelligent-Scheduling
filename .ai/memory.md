# [时间] 2026-08-31
# [定位] 实时上下文：当前 Sprint 进度缓存
# [作用] 记录"现在"正在做的事。任务完成后需及时清理。
# [规则] 重点标注 Tao 的进度。

## 📍 当前状态
- **08-31 周历导出图片（本次）**：✅ 已实现、浏览器实测通过 - 工具栏「导出图片」按钮（三维度均有）；html2canvas 离屏克隆截图（固定 1200px 宽 + 2x，窗口缩放无关）；标题区=周范围居中+视图标注（总览/任务：X/人员：X）+导出时间；白色圆角内容卡+四周留白；静态视图（克隆时移除今天高亮/旗子/智能排班按钮/表头建班次按钮）；空态防御（grid 未挂载 toast 提示）；测试数据已清理。Tao 主导了 5 轮设计迭代（固定宽度/视图标注/去今天效果/去功能按钮/留白卡片）
- **08-31 周历维度切换（上轮遗留）**：⏳ 已实现、67 单测通过、待 Tao 手测确认后统一提交 - 总览/项目/人员三维度（分段按钮+联动**可搜索**下拉）；项目维度全编辑；人员维度仅视图+请假入口；拖拽落点精确到卡片。设计由 Tao 拍板（人员仅视图、下拉要可搜索）
- **存储演进研讨**：⏸️ 搁置 - Tao 决定文件备份大块先不做（方案 A/B/C、B 档按周分桶延后）
- **dist**：⏳ 旧构建，提交后需 `node build.js` 更新（Tao 要求时才构建）

## 🧠 核心决策
- **导出图片方案定稿（08-31，Tao 迭代拍板）**：html2canvas（npm 依赖、esbuild 内联单文件、file:// 可用；**突破「外部依赖仅 SheetJS」约束**，Tao 批准）；离屏构造「浅底画布+白圆角卡+标题+周历克隆」整体截图（原页面零扰动）；固定 1200px 周历宽（窗口缩放无关）；导出=静态视图（移除一切功能性 UI）；详情已固化 spec.md 变更记录 08-31
- **维度切换设计定稿（08-31，Tao 拍板）**：分段按钮+联动可搜索下拉；项目维度保留全部编辑、**人员维度仅视图**（只读+请假入口）；维度状态 localStorage 持久记忆（`is_sched:cal_view`）；详情已固化 spec.md 变更记录 08-31
- **存储演进搁置**：文件系统备份（方案 A/B/C）、B 档按周分桶均延后，维持 localStorage 单 key 现状

## ⚠️ 待办与注意
- [待办] Tao 确认导出图片效果 → **统一提交**（维度切换+导出图片+html2canvas 依赖+三个 .ai 知识文件）→ 按需构建 dist
- [待办] 存储演进（方案 A/B/C、B 档按周分桶）搁置，待 Tao 重新定夺
- [注意] 导出图=静态视图：克隆时移除 `.cal-today`/`.cal-today-flag`/`.cal-add-day`/`.sch-smart`，勿在克隆里残留功能性 UI（Tao 指出：导出的是视图不是功能）
- [注意] 周历调试先查 `is_sched:cal_view` 维度状态：过滤视图下 DOM 卡片不全 ≠ 数据丢失（08-31 调试差点误判数据层丢数据）
- [注意] `ctx.weeklyFatigue` 锚定首个班次所在周（buildContext 用 `schedules[0]?.date`），跨周浏览的统计需按浏览周现算（08-31 摘要行即如此）
- [注意] 测试痕迹：08-24 有一个空「浇花·早」班次（拖拽测试构造同格双卡场景所建），可删
- [注意] 周历渲染不再自动展开：新任务需「批量铺排」或手动建班次才出现（spec 4.1）
- [注意] 改侧边栏样式需两处同步：index.html 首屏内联段 + theme.js 侧边栏段（pitfalls#14）
- [注意] 直接改 localStorage 会与 db 内存 Map 失同步：恢复数据后需刷新页面重新加载
- [注意] SheetJS 社区版 write 剥离单元格样式：模板示例行用「【示例】」前缀标识而非背景色
- [注意] 拖拽回归手测：Playwright 无法模拟 dataTransfer（pitfalls#4，用原生 DragEvent+DataTransfer 构造）
