# [时间] 2026-09-01
# [定位] 实时上下文：当前 Sprint 进度缓存
# [作用] 记录"现在"正在做的事。任务完成后需及时清理。
# [规则] 重点标注 Tao 的进度。

## 📍 当前状态
- **09-01 待提交批次（五块，均代码完成、65 单测通过，待 Tao 确认后提交）**：
  ①周历 UI 打磨五项（导出 hover 下拉/表头白色浮钮/右下角闪电图标/时段四色/标题截断）
  ②弹窗全局改造：右上角 X 关闭钮 + 遮罩点击不再自动关闭（modal.js 单点改造全覆盖，ESC 与 footer「关闭」保留）
  ③疲劳分析页重构：单指标紫柱图（详见核心决策），统计卡去悬浮，图表包白卡
  ④**请假概念移除 + 替换功能定稿**：点人名一步直达替换弹窗（当天全部班次分组 Top3，替换后重算其余组候选），不再维护 Leave 记录（详见核心决策）
  ⑤**基础配置卡片去悬浮**：theme.js 删 `.card:hover` translateY 上浮（人员/任务卡不再跳动；分析页统计卡 09-01 已去悬浮，同趋势）
- **存储演进研讨**：⏸️ 搁置 - Tao 决定文件备份大块先不做（方案 A/B/C、B 档按周分桶延后）
- **dist**：⏳ 旧构建，提交后需 `node build.js` 更新（Tao 要求时才构建）

## 🧠 核心决策
- **替换功能定稿（09-01，Tao 提议「标记请假不实用」）**：点击人名**一步直达**「替换」弹窗（合并原 staffDialog 中间层+替补弹窗）：头部姓名+状态徽标+周积分，当天全部班次分组卡（rpl-group），每组 Top3 候选（assign-* 行样式+「选此替补」hover 滑入），替换成功组绿底「✓ 已由 X 替换」，无候选灰化「无可替补人员」（不自动移除原人），当天无班次提示；单弹窗不叠加（原实现多班次循环叠开多个 modal）。**替换=一减一增，ctx 计数双向同步**（吸取 08-28 拖拽 ctx 不同步教训）；**零状态记录**：被替换人当天其余班次照常、仍可被排入其他班次（Tao 拍板放弃「当天空闲」硬约束，更灵活）；Leaves 全链路删除（模型/存储/JSON/过滤/buildContext 签名），旧备份 leaves 字段导入显式忽略
- **疲劳分析单指标定稿（09-01，Tao 三轮追问拍板）**：图表只展示累计劳累积分（紫柱单柱，高度=积分，同尺可比）。**否决史：双尺度红柱（高强度次数按最大次数缩放，红 1 次顶到紫 6 分高度，跨色不可比是误读根源）、个人周上限虚线（难理解）、高强度×N 姓名下胶囊（本页不展示高强度）**。Y 轴整数刻度自动扩展（默认 0-6 每格 1，超限扩 8/10/12，NICE 表驱动）；超限表达=柱顶红章「超限」+姓名红标加粗
- **弹窗交互规范定稿（09-01）**：openModal 统一 header 右上角 X（ICON_X，hover 浅紫底）+ 遮罩点击不关闭；ESC + footer「关闭」保留；全弹窗 modal.js 单点生效
- **时段四色体系定稿（09-01，Tao 4 轮迭代拍板）**：自主=藕荷灰粉 `#f2e5ee`、早=晨玫瑰粉 `#fde8ef`、中=正午雾蓝 `#e3edf8`、晚=暮紫 `#e4d6f4`（组内最深），任务卡边框 `--sb` 随时段色加深。**配色约束：跳出色系的绿/黄均被否，新配色须在莓果梅紫谱系（紫/粉/蓝冷调）内选择**
- **智能排班入口定稿（09-01）**：空班次卡片右下角 20px 闪电小图标（与「需 N 人」同行 space-between）；不放右上角（与时段 chip 同角落冲突）
- **存储演进搁置**：文件系统备份（方案 A/B/C）、B 档按周分桶均延后，维持 localStorage 单 key 现状

## ⚠️ 待办与注意
- [待办] Tao 确认本批次（周历打磨+弹窗改造+疲劳分析+替换简化）→ 提交 → 按需构建 dist
- [待办] 存储演进（方案 A/B/C、B 档按周分桶）搁置，待 Tao 重新定夺
- [注意] 替换弹窗候选列表是「打开时计算」的快照，**每次替换成功后必须重算其余未完成组的候选**（ctx 已变化，否则替补者可能被塞进超限班次——浏览器实测发现并已修复）
- [注意] 替换功能不产生状态：被替换人当天仍可被排入其他班次（无「当天空闲」硬约束），领导需逐班次替换；人员维度视图点击人名=替换入口
- [注意] 高强度次数已无全局可视化位（分析页单指标化），系统内仅排班分配弹窗可逐班次查看；Tao 认可现状，未来如需可补轻量展示位
- [注意] 导出图=静态视图：克隆时移除 `.cal-today`/`.cal-today-flag`/`.cal-add-day`/`.sch-smart`，勿在克隆里残留功能性 UI（Tao 指出：导出的是视图不是功能）
- [注意] 周历调试先查 `is_sched:cal_view` 维度状态：过滤视图下 DOM 卡片不全 ≠ 数据丢失（08-31 调试差点误判数据层丢数据）
- [注意] `ctx.weeklyFatigue` 锚定首个班次所在周（buildContext 用 `schedules[0]?.date`），跨周浏览的统计需按浏览周现算
- [注意] 改侧边栏样式需两处同步：index.html 首屏内联段 + theme.js 侧边栏段（pitfalls#14）
- [注意] 直接改 localStorage 会与 db 内存 Map 失同步：恢复数据后需刷新页面重新加载
- [注意] 拖拽回归手测：Playwright 无法模拟 dataTransfer（pitfalls#4，用原生 DragEvent+DataTransfer 构造）
- [注意] 测试痕迹：08-24 有一个空「浇花·早」班次（拖拽测试所建），可删
