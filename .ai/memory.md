# [时间] 2026-08-27
# [定位] 实时上下文：当前 Sprint 进度缓存
# [作用] 记录"现在"正在做的事。任务完成后需及时清理。
# [规则] 重点标注 Tao 的进度。

## 📍 当前状态
- **配置页卡片化 + 休假状态 + 开关**：✅ 完成（待提交）- 人员/任务列表表格改卡片网格；人员新增 `rest` 休假状态（卡片开关切换，restFrom 记录/恢复原状态，退出仅编辑弹窗）；任务卡启用/停用开关；排序=已退出殿后+加入时间倒序；劳累指数 emoji 🔥 换 lucide 火焰 SVG（config+calendar 两处）；编辑按钮 ghost 样式；单测 63 全绿 + Playwright 实测
- **侧边栏修复**：✅ 完成（待提交）- ①收缩态品牌区高度 16→13 对齐；②FOUC 首屏样式内联 index.html（与 theme.js 双源）
- **时段体系重构（去时间）**：✅ 完成（待提交）- 时段=自主安排/早/中/晚纯标签；数量上限+预警替代时间冲突；周历 4 行；旧数据清空重配
- **存储演进研讨**：⏳ 进行中 - 桌面文件存储不可能三角；方案 A（每次点目录）/ B（OPFS 无感）/ C（本地应用）待 Tao 定夺
- **B 档优化**：⏳ 待研讨 - 按周分桶消除 schedules 单 key 无界增长（撞 5MB）；版本号 + 迁移机制
- **dist**：⏳ 旧构建，需 `node build.js` 更新

## 🧠 核心决策
- **配置页卡片网格**（本次）：`.card-grid` 响应式多列 + `.cfg-card`（头部=名称+状态徽标，字段行标签化，操作区=开关+编辑）；`.btn-ghost` 编辑按钮（项目已有类，直接复用）
- **休假=开关、退出=编辑弹窗**（本次，已入 spec）：人员状态机 new/active ↔ rest（开关可逆，restFrom 记录休假前状态，new 休假恢复仍为 new 不自动转正）；left 仅编辑弹窗单向最终；filter 硬性排除 rest，团队平均排除 rest
- **排序规则**（本次）：已退出殿后，其余按 joinedAt 倒序（新加入在前）；旧数据无 joinedAt 自动殿后
- **开关交互局部更新**（本次）：切换开关只更新徽标+标签文本，不 renderConfig 重建 DOM（避免 hover 上浮抖动 + 全页闪烁）；编辑弹窗保存仍全量重渲染
- **config tab 持久化**（本次）：`is_sched:config_tab` 记住当前 tab，刷新后停留原 tab
- **存储数据流铁律**（长期，已入 spec）：cache 唯一真源，视图经 getCache() 读；saveXxx 增量更新缓存不重读；loadAll 仅初始化/导入/重置
- **db 内存 Map 索引**（长期，已入 spec）：O(1) CRUD、首次访问才从 localStorage 加载；writeAll 一次整表替换；局限=多标签页不同步（单标签场景无碍）
- **file:// 存储边界**（长期约束）：showDirectoryPicker 可用但目录句柄无法跨会话持久化；「自动+固定路径+免授权」不可能三角
- **首屏样式双源**（长期）：侧边栏首屏样式内联 index.html 消除 FOUC，theme.js 仍注入全量覆盖；**改侧边栏样式需两处同步**（注释锚点已标记）
- **数量上限+预警**（长期，已入 spec）：时段标签均无时间；按「一人一天最多 N / 一人一时段最多 M」硬性过滤；达预警阈值 chip 黄+toast

## ⚠️ 待办与注意
- [待办] 提交积压 3 笔：侧边栏修复、时段体系重构、卡片+休假改造（含 spec/memory/icons.js），视改动量合 1-3 笔
- [待办] 存储演进定案：A 档（方案 A/B/C）+ B 档（按周分桶/版本迁移）研讨
- [待办] dist/index.html 旧构建，需 `node build.js` 更新
- [注意] 时区陷阱已入 pitfalls：本地凌晨/周切换会踩
- [注意] 拖拽回归手测待做：Playwright 无法模拟 dataTransfer（pitfalls#4）
- [注意] 样式 JS 注入有 FOUC：首屏静态部分需内联 index.html（pitfalls#14）
- [注意] 开关/卡片操作勿全量 renderConfig 重建：局部更新防抖动（本次踩坑，待 Tao 确认入 pitfalls）
